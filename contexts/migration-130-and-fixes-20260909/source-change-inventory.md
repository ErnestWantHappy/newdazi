# P0-A 只读体检：源码与改动台账（2026-09-09）

基线：HEAD `c3d4b22`（分支 `codex/research-activity-v1`），工作区改动全部保留，未 stash、未丢弃。

## 1. 未提交跟踪改动（6 个文件：5 个既有 + `.gitignore` 本轮排除项）

| 文件 | 规模 | 内容与影响 |
| `RuoYi-Vue3/.../classroomDesktop/index.vue` | 大改（视觉重构+每排人数） | 2026-09-09 已审查：无新增接口、无缺绑定；补保存失败提示、去空类名后**纳入 130 版本**。 |
| `RuoYi-Vue3/src/views/student/index.vue` | 小改 | 学生首页减负（与 09-08 去套娃配套、尚未发布）：进页不再对每题打 ENTERED，作答时才补记；静默轮询跳过协作当前/历史。线上仍是旧风暴行为，需另打前端包。这是已知的线上必要修复，迁移制品应包含。 |
| `contexts/PROJECT_CORE.md` | +23 行 | v3.44→v3.46：补 130 规划、去套娃基线。文档改动，无业务风险。 |
| `docs/architecture/INDEX.md` | +4 行 | 130 规划入口与红线提示。文档改动。 |
| `docs/architecture/DEPLOYMENT_RUNBOOK.md` | +8 行 | 去套娃拓扑（80 静态直出、3010 共存、1024 硬顶）。文档改动。 |
| `.gitignore` | +3 行 | 本轮新增：排除 `快速部署.ps1` 提交。无业务影响。 |

## 2. 未跟踪文件（均保留未动；`快速部署.ps1` 已 gitignore 排除提交）
| 文件 | 说明 | 处置 |
| --- | --- | --- |
| `快速部署.ps1`（仓库根，09-07，约 4.8KB） | 旧直连部署脚本：经共享拷贝 JAR/dist，含停服务逻辑。**含 123 明文口令，属凭据泄露风险；且绕过发布门禁（无指纹、无清单、无备份校验）。** | 2026-09-09 已在 `.gitignore` 明确排除提交（文件本身未删、未执行）。仓库内无引用。若曾外发，轮换口令需另行确认。 |
| `contexts/linux-130-bootstrap-handoff.md` | 130 初始化交接（含旧分区/格式化计划）。系统层已完成，**禁止重跑其中格式化/安装命令**。 | 保留作废计划备查；后续步骤引用现场证据而非重跑它。 |
| `contexts/migration-130-and-fixes-20260909/` | 本轮需求/设计/任务/提示词/术语 + 新增体检产物。 | 保留；P0-A 产物为 `environment-audit.md`、`source-change-inventory.md`（本文件）。 |

## 3. 线上差异结论

- 线上前后端 = `20260907_blankfix_v1`（哈希已在环境审计中复核一致）。
- 本地相对线上的业务增量至少含：学生首页减负（应带）、课堂大屏大改（待审）。
- 按 design.md 要求：迁移制品必须基于“最新源码全量构建”，附版本/源码清单/哈希/SQL；
  不采用旧 JAR 拼类，不盲目混入未完成工作。课堂大屏大改的包含与否是 P0-E 前必须书面决策的一项。
- 需求文档 §3 提到的设计器 `addLessonTool` 缺失、`updateTopic` 已存在、匿名分享边界、Python 权重校验等只读结论，
  本轮未改动对应源码，保持原判。
