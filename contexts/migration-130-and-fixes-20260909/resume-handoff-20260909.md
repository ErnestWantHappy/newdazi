# 130迁移中断交接提示词（2026-09-09）

把下面整段交给新的 AI。它不是重新设计方案，而是接着当前现场继续做；先核实状态，不能重复导库、重复传输或误删中间文件。

---

你正在接管“信息科技学业测评平台迁移到10.52.1.130”任务。当前任务目标是：在不破坏10.52.1.123现有正式平台的前提下，把本机已验证的最新前后端、123完整业务数据库和题目/作品/附件文件迁到130；在130完成真实验收后，才由用户切换域名。

## 一、必须先读的资料

按顺序读取：

1. `AGENTS.md`
2. `contexts/PROJECT_CORE.md`
3. `docs/architecture/INDEX.md`
4. `contexts/migration-130-and-fixes-20260909/requirements.md`
5. `contexts/migration-130-and-fixes-20260909/design.md`
6. `contexts/migration-130-and-fixes-20260909/tasks.md`
7. 本文件

读取后运行 `git status --short`。工作区已有大量用户修改，全部保留；不得 `reset`、覆盖、清理未跟踪文件或把临时迁移产物误提交。

## 二、不可突破的边界

1. 不修改、不覆盖、不停止、不重启123的 `NewDaziBackend3009`、`UnifiedNginx`、活动 release、Nginx/NSSM 配置和生产业务数据库。
2. 123只能用于只读核对，以及在明确的独立备份/中转目录存放迁移制品。禁止调用针对123生产发布的 `scripts/deploy.py deploy/rollback`。
3. 不修改、不重启、不踢掉129正在服务123的 CryptPad、Judge0、EMQX；不要复用生产 MQTT ClientID，不向129写生产测试数据。
4. 可以修改和重启130自己的服务、数据库、Nginx、Redis、目录和配置。
5. 用户允许最终切换时安排短暂停止业务写入，但具体时间和实现方式仍须另行确认。当前不能因为“现在看起来没人用”就把本次旧备份宣布为最终一致。
6. 不切换DNS。只有G2验收通过后才告诉用户“可以切域名”，域名由用户操作。
7. 凭据只从 `contexts/secrets.local.md` 使用，不打印、不写入代码、脚本、日志或交接文档。

## 三、上一位 AI 已经做过的事情

以下是可复核的事实，不要重复执行：

### 1. 项目文档和本地代码

- 已创建并维护专题目录 `contexts/migration-130-and-fixes-20260909/`，其中已有需求、设计、任务、新AI提示词、环境审计、日志审计、文件清单和外置配置模板。
- 已更新 `contexts/PROJECT_CORE.md` 与 `docs/architecture/INDEX.md`，明确130迁移进行中、123保护红线和用户允许最终短暂停写。
- 已保留工作区原有课程删除、学生删除、菜单、流程图、教研分享、工具、Python等改动。
- 本轮额外修正了Linux LibreOffice进程统计/受控清理逻辑，并为迁移期间的启动恢复增加了配置开关；后端完整构建通过，497项测试失败数为0、错误数为0；前端 `npm run build:prod` 已成功。不要因为行数多就擅自回退课堂大屏等已有修改。
- 新的构建产物与哈希记录在 `output/migration-release-20260909/manifest.json`，但要以当前源码重新核对，不能盲信旧制品。

### 2. 123现状

- 通过固定SSH指纹和123跳板做过只读检查。
- 123的 `NewDaziBackend3009` 和 `UnifiedNginx` 仍为运行状态；活动线上版本基线没有改变；没有执行生产发布、重启、SQL或配置切换。
- 123的备份/中转目录只新增了迁移专用目录：`D:\program\3009dazipingtai\backups\codex_resume130_20260909`。不要删除它或旧备份。
- 为完成打包和直传，123上曾创建一次性任务 `Codex130AssetPack20260909`、`Codex130DirectTransfer20260909`；它们不是生产服务。先只读确认任务状态和输出后，在确认不再需要时再清理临时任务/密钥文件，不能误删迁移归档。
- 123仍可能继续产生 `sys_job_log` 和其他业务写入。因此已有备份只能作为演练/中间快照，不能直接作为最终切换快照。

### 3. 130数据库恢复情况

- 通过123跳板把数据库备份传到130：`/data/backups/migrate-130-20260909/ry-vue_full.sql`。
- 130备份文件SHA-256：`cb1e44eee34cf857951b0f3e1c2488869eb5d1cd1c218bc57663a8a1456dc1f6`。
- 备份已恢复到130的正式库 `ry-vue`，不是测试库；恢复后存在143张表。
- 逐表行数检查：业务表与123快照一致；唯一已见差异是 `sys_job_log` 少24条，这是123在备份后继续写入的日志，不是恢复失败。
- 关键值：`biz_student_answer=213750`、`MAX(answer_id)=339244`、`sys_user=27012`。重新核对时以现场实际值为准。
- 菜单SQL和清除按钮SQL已经在130的 `ry-vue` 执行并复核：教师顶级菜单9项、成绩查询子项2项、教师工具子项4项、彻底清除按钮2项；原教研员工具权限已补继承。执行过的脚本是 `sql/teacher_menu_group_v1.sql` 和 `sql/purge_buttons_v1.sql`，脚本备份在130迁移目录中。
- 不要把130的测试账号、测试学生、测试成绩从 `ryvue_test` 带入正式库。正式切换前检查 `ry-vue` 是否存在测试数据。

### 4. 130当前服务状态

- 130的 `xueyeceping-130` 与Nginx当前均为active，80端口和 `/prod-api/captchaImage` 返回200。
- 当前运行版本仍是 `/data/apps/xueyeceping/releases/20260909_migration_resume_v1`，它的外置数据库配置仍指向 `ryvue_test` 测试库；这意味着130当前网页验收不是正式数据验收。
- 已准备但尚未切换运行的 `/data/apps/xueyeceping/releases/20260909_migration_resume_v2` 不能直接当作已上线版本。切换前必须重新核对JAR、前端、配置和服务单元。
- 130隔离验收曾通过：登录、`getInfo`、`getRouters`、诊断接口均200，教师首页无页面错误；这是测试库证明，不是正式迁移完成证明。
- 130诊断曾返回 `ryvue_test` 数据源。正式库切换后必须重新打开诊断中心，确认数据源是 `ry-vue`、主机是Linux 130，历史日志来源单独标识；不能把123历史日志冒充130实时状态。

### 5. 文件迁移状态

- 123上的文件已打成 `/data` 迁移归档对应的源包，清单记录 **47,520个文件、8,437,661,204字节**。
- 完整归档大小 **8,522,711,040字节**；归档SHA-256：`92067b4cc75b35513228ca2b0488951e98419b930a2b4244098f34f86bdf4a14`。
- 归档在130的路径：`/data/backups/migrate-130-20260909/assets.tar.direct.partial`。文件大小和SHA已与源包核对一致。不要删除、覆盖或重传。
- 归档来源清单：130迁移备份目录内的 `files.jsonl`；源端打包状态显示 `status=complete`。
- 解包尝试写入隔离目录 `/data/backups/migrate-130-20260909/extracted`，没有写入正式 `/data/upload`。
- 解包因一个历史文件名超过Linux单个文件名长度限制而失败。失败文件名来自旧上传数据，不能简单认为是迁移脚本错误，也不能直接截断后覆盖原文件。保留失败目录和日志，先统计所有超长/非法路径。

## 四、你现在必须完成的工作

### P0：接续文件迁移，不重复传输

1. 只读检查归档、`files.jsonl`、`verification-status.json`、`extract.log`和已解包目录。
2. 统计所有路径组件长度超过Linux 255字节、总路径过长、非法字符、重复路径、符号链接和硬链接；输出 `long-name-report.json`。
3. 对归档采用流式安全解包，禁止再次调用会直接失败的 `tar.extractall`。有效文件先解到专用 staging 目录，逐文件按清单核对大小和SHA-256。
4. 对超长文件名：
   - 先查它们是否被数据库字段引用；
   - 未被引用的文件放到明确的隔离/待治理目录并记录，不伪造成正常文件；
   - 被引用的文件必须生成稳定的哈希文件名映射，更新数据库前做dry-run、备份、变更计数和引用复核；保留原始显示名/映射表，不能随意截断或改成空文件；
   - 如果发现路径对应的是恶意/异常上传内容，先隔离并在报告中标明，不执行文件内容。
5. 只有全部可迁移文件和超长文件映射有明确结论后，才把 staging 切入 `/data/upload`。切换前再次核对权限、拥有者和相对层级。
6. 最终输出：总文件数、总字节、成功数、源端既有缺失数、超长文件数、数据库引用数、迁移失败数和SHA抽样/全量结果。

### P0：把130从测试库切到正式库

1. 先备份130当前服务单元、Nginx配置、v1/v2配置和正式库当前状态；不要覆盖任何回退文件。
2. 修正130 release外置配置，把数据库URL由 `ryvue_test` 改为 `ry-vue`；确认上传根为 `/data/upload`，LibreOffice为Linux路径，Redis为130本机。
3. 确认迁移隔离开关真正生效。环境变量命名可能无法正确映射到带连字符的Spring属性，不能只看文件里写了变量；用启动日志、Quartz状态和数据库只读查询证明：迁移验收期间不会自动恢复遗留判题/AI/Office任务，不会启动MQTT生产订阅，不会向129重复发任务。
4. 130正式库切换只重启130自己的 `xueyeceping-130`；重启前验证主机是10.52.1.130，禁止对123执行同名服务命令。
5. 130的Nginx root切到当前验证过的release，`/prod-api/`和WebSocket代理配置通过 `nginx -t` 后再reload；正式服务验证前不要切域名。

### P0：正式库完整一致性复核

1. 重新读取123当前时间点、关键行数、最大答案ID、文件引用统计；不能把旧快照和当前源端混称一致。
2. 用户已允许最终短暂停写，但具体窗口和停写机制仍需再次确认。正式切换前必须重新生成最终数据库备份和文件增量，处理快照后的新增记录。
3. 备份恢复后逐表核对；对日志表增长要说明时间差，对业务表差异必须逐项解释。
4. 对所有数据库文件引用执行存在性检查，区分源端原本就缺失与本次迁移遗漏；不得生成占位文件。

### P1：130真实业务验收

在正式库切换后，按顺序做并留证：

1. IP访问 `http://10.52.1.130`、前端静态资源、登录、教师首页和菜单。
2. Word/Office真实文件上传、Linux LibreOffice转换、中文内容和下载/预览。
3. 流程图创建、保存、刷新、提交、批改；菱形和平行四边形尖角截图确认。
4. Python题目导入、非等权重测试点、实际提交到Judge0；只做隔离的一次验证，不压垮129。
5. 课程工具添加、保存、刷新、学生入口。
6. 教研通知编辑；匿名分享正文和留言分页，坏令牌404，隐藏留言不泄露。
7. 成绩查询、成绩画像、学生管理、课程管理、彻底清除权限和跨课引用保护。
8. 协作必须验证真实打开、双人编辑、保存刷新、权限隔离；如果仍依赖129入口未能验证，不能宣布130完全独立可用。
9. 系统诊断中心：数据源为 `ry-vue`、应用主机为130、Redis和Office为130本机，129依赖单独显示。

## 五、完成标准

只有满足下面条件才能向用户说“130可以切域名”：

- 123两个服务仍正常，且本轮没有改123生产服务、配置、数据库或活动release。
- 130正式库和全部文件完成一致性复核；超长文件已有明确处理结果。
- 130应用不再连接`ryvue_test`，而是连接`ry-vue`。
- Word、流程图、Python、课程工具、菜单、教研分享/通知、成绩和诊断中心完成真实验收。
- 迁移期间的后台副作用任务不会重复处理123历史数据；正式切换后才按计划启用。
- G2报告写明最终数据时间点、备份SHA-256、文件差异、回滚方式、访问IP和域名/端口。

不能因为80端口200、登录成功或测试库网页可用，就宣布130正式可用。

## 六、失败和回退规则

- 文件异常：保留归档、staging、失败清单，不能删除源端或覆盖正式上传目录。
- 130服务异常：恢复130自己的服务单元/Nginx配置和release；不要碰123。
- 130正式库已接收新数据后，不能直接把DNS切回123而丢失新增成绩。先停新写入、备份并对账，再制定回迁。
- 迁移尚未完成时，不切DNS、不停123、不删除旧备份。

## 七、每次汇报格式

用简体中文说明：

1. 本次做了什么；
2. 证据和校验值；
3. 哪些已完成、哪些仍阻塞；
4. 123和129是否被写入（应为“没有”）；
5. 130当前release、数据库、文件状态；
6. 下一步只做什么。

先执行“P0接续文件迁移”的第1、2步，完成超长文件报告后再继续；不要重复数据库恢复、不要重复8GB传输，也不要等待用户重新提供密码。

---
