# P0-B 只读日志排查：事件审计（2026-09-09）

时区：123 系统时钟 `2026-09-09 14:07 +08:00`（China Standard Time），与东八区一致，日志时间可直接用。
范围：123 生产 `sys-error.2026-09-08.log`（17793 行）为主；09-07 文件日志接近空（0KB），
08-25 文件日志已随旧 release 轮转消失（最早仅到 09-02），08-25 取证走数据库（见下）。
Nginx `error.log` 自 09-08 14:30 去套娃重启后无新增（502 危机已止）；`access.log` 经探活写入正常。
全程只读，未导出学生隐私内容。

## 1. 2026-08-25 朱屹教师 Python 导入（已定因方向）

- 用户定位：`sys_user` 中朱屹为 `user_name=675077`（dept 171）；15:11/15:16 两次登录成功。
- 当天该账号仅 4 条操作：15:13 课程保存、15:28:50 题库新增、15:29:26/34 两次题库修改，
  全部 `status=0`（成功）。当天全库仅 20 条操作日志、0 失败。
- 实际落库：仅 1 题，`question_id=2004`，类型为 **practical（操作题）**，
  内容“将海龟画图代码粘贴到平台上并运行”，15:28:50 建、15:29:35 改。
- 关键矛盾：用户描述为“Python 导入”，实际建成的是操作题而非 Python 题；
  且 `sys_oper_log` 历史上从无 `oper_url LIKE '%python%'` 记录（Python 导入接口未记操作日志）。
- 结论：不是权重合计 bug（后端已有合计 100 校验），最可能的是**导入入口/模板把 Python 题建成/归类错误、
  或用户在题库手工建题时选错题型**。修复按 F08 走“模板下载→解析→预览→导入→判题”逐层比对，
  重点查导入题型映射，不动后端权重语义。需向用户要当时的导入文件才能100%定因（见 F08）。

## 2. 2026-09-08 高频错误分级

| 级别 | 现象 | 证据 | 结论 |
| --- | --- | --- | --- |
| P1（真缺陷） | 旧前端 `room/undefined/leave` 参数类型不匹配 | 3 次，u=15189 | 已知 `roomId=undefined` 跳转 bug，修复在本地未发布。随前端制品走，见 F-前端。 |
| P1（真缺陷） | LibreOffice 进程超时 `ensureProcessExited` | 约 12 次，全天零散 | Windows Office 不稳定，与既有“多次崩溃自动拉起”一致。根治靠 130 迁移（已验证中文转换正常）。 |
| P2（观察） | `/login` 用户不存在/密码错误 | 零散，匿名；14:27 有 5 次 20 秒集群 | 普通输错密码；集群需结合登录防护看是否同一 IP。本轮不修，仅记录。 |
| 噪音 | `current-lesson`、`resourceView` 的 `AsyncRequestNotUsable` | 约 58 次，集中 13–16 点课堂时段、多用户分散 | 均为浏览器中途关页面/跳转导致连接中断，不是服务端 bug，不修（修了也没用）。 |
| 预期行为 | `AccessDenied`（仪表盘/抽测/题库） | 零星 | 权限拦截正常生效，不修。 |
| 单发 | 协作 `workspace` 数据库操作失败 | 1 次，u=1906 | 孤例，待 130 协作验收时观察，不 suspend。 |

09-07 白天：文件错误日志为空；当天的 502/连接不足已在去套娃记录中定因，无需重复。
班级唯一键冲突、重复登录循环在本批错误日志中未见新增聚集，维持原判（待 130 压测观察）。

## 3. 待补证据

- 朱屹当时的导入源文件（Excel/模板）：没有它，F08 只能做到“逐层加固+错误定位”，不能复现原故障。
- 14:27 登录集群的来源 IP：如需定性（误操作 vs 撞库），可再查 `sys_logininfor`，本轮未列为阻塞。
