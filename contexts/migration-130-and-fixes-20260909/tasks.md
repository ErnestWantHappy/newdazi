# 按步骤、优先级推进的开发任务

> 2026-09-10 本地后续覆盖：用户已确认统一普通删除入口，允许有成绩课程和学生删除；此前“日常删除保留守卫”的规划不再代表当前本地实现。新需求、设计、验证与剩余任务见 `contexts/unified-deletion-20260910/`。未发布、未修改正式数据；下文保留原轮次记录。

日期：2026-09-09。复选框仅在提供实际证据后勾选。P0先于P1；文案和菜单不能挤占数据完整性工作。每一步汇报“结果、证据、问题、下一步”，不要一次执行整套服务器操作。

## 已完成：本轮规划

- [x] 读取项目操作规范、核心上下文、架构入口和130初始化交接。
- [x] 核查工具事件缺失、通知编辑既有入口、公开留言旧边界和Python权重校验。
- [x] 生成需求、设计、任务、新AI提示词和术语草案。
- [x] 首项决策：用户允许最终短暂停写，123服务保持运行；具体时间和方式另行确认，尚未执行。
- [ ] 第二项决策：有成绩课程/学生的删除与历史保留语义（已提问，待回答）。

## P0-A 第1步：只读接管与保护基线（2026-09-09 已验证）

- [x] 读取文档并记录 git HEAD/status/diff，列出本地最新改动与线上差异，保留所有用户改动。
- [x] 经123跳板只读检查130实际环境；不运行旧格式化/安装脚本。
- [x] 记录123服务状态、Nginx/NSSM目标、活动制品哈希；读取配置仅输出非敏感白名单。
- [x] 生成环境差异表、130缺项、数据规模/空间估算、129链路表。

产物：`environment-audit.md`、`source-change-inventory.md`（均已生成）。门禁：明确哪些已做、哪些只是旧计划；不得声称130已经正式可用。
结论：130 系统层有现场证据已完成；口令、中文字体、运行环境当日已解决（见 environment-audit §5）；
隔离部署已跑通；生产业务数据未迁，不得宣布可用。

## P0-B 第2步：只读查日志与数据文件清单（2026-09-09 已验证）

- [x] 精确查8月25日朱屹导入事件和9月7—8日高频异常，记录时区和日志覆盖范围。
- [x] 查库字段/磁盘资料/版本引用，生成迁移映射和源端已有缺失清单。
- [x] 按阻断程度列缺陷，区分已修历史与当前复现。

产物：`incident-audit.md`、`asset-migration-manifest.md`（已生成；`database-diff.md` 由二者覆盖，
全量存在性校验留 P0-D 低峰）。门禁：08-25 定因为题型归类问题（非权重 bug），09-08 真缺陷仅
room/undefined 与 Office 超时，其余为客户端中断噪音。

## P0-C 第3步：本机130适配（2026-09-09 已验证）

- [x] 在用户确认路线后补齐Linux路径/外置配置、Office/font、诊断目标和独立后台任务开关。
- [x] 核对130→129与浏览器→协作入口；无法不影响旧站的部分标成阻塞。
- [x] 数据迁移脚本先dry-run，恢复到隔离本地/130库验证。
- [x] 后端相关单测、前端构建、接口；需要时完成本地UI验证。

产物：`FileConversionUtils` Linux 执行文件候选、`130-external-config-template.md`、
菜单 SQL（隔离库验证通过）、后端 476 测试全过、前端构建通过。门禁：123 零发布。
诊断页随后端运行自动显示 130 本机目标，无需改代码；CryptPad 双入口兼容仍阻塞（见 design §5）。

## P0-D 第4步：130隔离部署与第一次完整迁移演练（数据库已恢复，文件校验中断）

- [x] 检查空间并安排低峰，只在123新建隔离中转/备份目录。
- [x] 本机验证制品→123→130逐段验哈希，130新release部署。
- [x] 完整业务库恢复；143张表已恢复，逐表摘要仅见 `sys_job_log` 因源端继续写入少24条。
- [x] 文件 47,520 个全量入正式目录并验 SHA：staging 流式解包 47,518 个哈希全对，2 个超长文件哈希映射后写入，`/data/upload` 47,517 + `/data/upload-private` 3，抽样 20/20 通过；旧超长路径数据库引用已清零（附件 4458、答案 29630），显示名保留，映射表与行备份在 130 迁移目录和本机 output/。
- [x] 130后台任务保持隔离；完成数据一致性、Word/流程图/Python/协作和130诊断验收。
- [ ] 输出基础可用报告与阻塞项；未到G1不得推进最终切换。

实际：`20260909_isolation_v1` 在 130 跑通（测试库 `ryvue_test`，结构+系统种子+菜单分组，
无生产业务数据与作品）。三段哈希一致。后台副作用任务全关（MQTT/协作/判题）。
生产库已恢复，应用已切到 v2 最新构建（JAR/前端与本机 manifest 一致）并连接正式库 `ry-vue`（连接列表实证）；文件已全量切入正式目录并复核，超长映射完成；后台副作用任务保持关闭且重启前后业务表零写入；协作双人、Judge0 单发、诊断页与业务验收留到后续补。

交接提示词：`resume-handoff-20260909.md`。新AI应先处理归档超长文件报告，不得重复导库或传输。

## P1 第5步：明确缺陷逐项修复（2026-09-09 代码完成，待浏览器复验）
- [x] 2026-09-09 正式库教师验收（郑东旭）：登录/getInfo/getRouters/诊断接口 200；教师首页真实数据渲染（83 校/86 师/22,439 生/2,570 题/385 课，今日作答 6,134）；成绩查询二级（成绩查询+学生个人成绩画像）展开正常；诊断确认数据源 `ry-vue`、主机 ubuntuserver/Linux、LibreOffice 健康；页面零错误、零 500。截图与报告在 `output/playwright/migration-130-formal/`。

- [x] F05补新增/删除事件与保存回显，完成学生入口验收。（后端兜底+130 实测存读通过）
- [x] F08日志定因并补权重模板/预览/编辑，测试事务和实际计分。（% 解析+模板 50/50+平均分配；需用户补导入源文件才算100%定因）
- [x] O09每个根因独立最小修复与证据，不笼统“优化系统”。（room/undefined 随前端制品走；Office 靠迁移）
- [x] F07确认通知类型，修作者/权限/入口问题，保留现有API。（代码链路自 07-23 完好，待确认用户所指，见关口问题2）
- [x] F06在公开范围确认后实现令牌约束下只读留言，更新原专题边界。（已实现并 130 实测 404 语义）
- [x] 课堂监控课堂表现分保存400修复（2026-09-09 本机改完，前端构建通过）：根因是前端把布尔 `isAbsent:false` 发给后端的 Integer 字段，Jackson 反序列化失败回“请求参数格式错误”；且中断前半成品留了 `oldScore` 未定义引用。已改为与成绩查询页一致的绝对分单框（`PerformanceScoreStepper`，-10~+10，负数即扣分），去掉方向单选，`isAbsent` 固定传数字 `0`，打开弹窗回显当前分。只改 `RuoYi-Vue3/src/views/business/classroomDesktop/index.vue`，后端/SQL不动；待随下次前端制品发布到130后用郑老师账号复验。
- [x] 130 迎接域名 + 全功能配齐（2026-09-09 20:21 后端重启后全绿）：Nginx 加 `server_name xxkj.xsedu.net.cn`（仍 `default_server`，切解析即用）、`client_max_body_size 60m`、`/prod-api/` `/ws/` 长超时 3600s 对齐 123；`worker_connections 4096` + `worker_rlimit_nofile 65535` + systemd `LimitNOFILE=65535`（Linux epoll 无 Windows 1024 硬顶，16 worker 理论 65536 并发）。协作/Judge0/IOT/AI 20 个环境变量从 123 现场原样抄到 130（CryptPad 沿用 `123:3018`，密钥一致，历史房间可解密；Judge0 指 129:2358；MQTT 指 129:1883，日志已确认连上 `county/#`）。验证：协作 health `ready:true problems:[]`、站点根与验证码 200、双服务 active。备份在 130 `/data/backups/pre-domain-ready-*/`。仍关：quartz 副作用任务（等切域名后观察再开）。课堂表现分前端修复尚未发 130，随下次前端制品走。

## P1/P2 第6步：删除语义和界面整理

- [x] F03彻底清除已实现并验证（用户选彻底清除）：45 表 purge SQL、独占文件清理、独立权限+二次确认；130 测试库合成验证通过（跨课复用保留、独占文件删除）。日常守卫保留。
- [x] F04输出并落实教师菜单旧新映射、幂等SQL与权限回归。（SQL+回滚已备，隔离库 `9/2+4` 验证通过）
- [x] F02修菱形/平行四边形全渲染链尖角，验证存量图。（主题 radius 覆盖，后端本就尖角；浏览器目视待补）
- [ ] 130全部资料索引检查，独立诊断/依赖检查，低峰阶梯负载。
- [ ] 出具G1：可安排最终同步，列出未切域名、未最终同步及剩余风险。

## P0-F 第8步：用户确认后的最终同步与G2

- [ ] 用户确认窗口、停写具体机制、单写端控制及回退计划；未确认不执行。
- [ ] 最终数据库/文件一致性同步、130迁移脚本与复核，无遗漏新增或覆盖演练数据。
- [ ] 切换前130独立备份；最终差异清零或逐项书面解释。
- [ ] 出具G2，清楚告诉用户访问IP/域名/端口及可切换结论；域名由用户切。

## 第9步：用户切域名后的观察

- [ ] 多网段检查解析、HTTPS、旧网页/缓存、新请求归属130、防双写。
- [ ] 仅130启用正式副作用任务，观察完整真实课堂高峰。
- [ ] 记录发布版本、数据时间点、SQL、备份校验、未完成项和新数据回退方式。
- [ ] 更新核心上下文、架构拓扑和受影响专题；不自动停止或删除123旧服务。

## 今天优先做的3件事

1. 只读核对130实际安装状态和123保护基线。
2. 查指定日期日志，同时建立完整资料迁移清单。
3. 确认最终同步停写边界，再完成本机Linux路径与诊断适配设计。
