# 信息科技学业测评平台 (Context)

> **版本**：v2.33
> **更新时间**：2026-07-21  
> **核心定位**：中小学信息科技 **教学 + 多维度测评**（选择/判断/操作/打字、批改、学情、导学单、区域抽测）。  
> **文档用途**：多 AI / 人工接力的 **业务真相**；操作纪律见 `AGENTS.md`。  
>
> **当前焦点（紧急修复已发布）**：2026-07-21 学年切换导致课程错届与成绩无权限已按 `biz_lesson.entry_year` 稳定锚点方案完成修复并发布至内网 3009/3010。正式库 227 门课程已回填且字段为 `varchar(4) NOT NULL`；教师首页、成绩列表/汇总/分析/矩阵及教师/教研员/学生页面已复验通过。后续开发不得再用动态当前年级反推课程届别，`grade` 仅是创建时内容年级快照；回滚旧 JAR 前必须先执行随发布保留的数据库兼容回滚 SQL。

---

## 一、技术边界

| 路径 | 说明 |
| :--- | :--- |
| `RuoYi-Vue/` | 后端（`ruoyi-admin` 启动，`ruoyi-business` 业务） |
| `RuoYi-Vue3/` | **唯一前端** |
| `sql/` | 增量 SQL |
| `contexts/context.md` | **本文件（唯一业务真相）** |
| `output/playwright/` | 系统冒烟脚本 + 最近截图 |
| `output/stress/` | 压测/平台验收主脚本 + 正式报告 |

**不要默认改**：`RuoYi-Vue/ruoyi-ui/`（旧 Vue2）；无必要大重构/换栈。

**Agent 默认行为**：先读本文件顶部焦点与「已定产品结论」；小改直接做，方案级（换栈/大改表/大重构）先给 2～3 方案再动手；**默认本机**；不 push、不连生产写库除非用户授权。

---

## 二、已定产品结论（2026-07-15，以代码为准）

### 2.1 课程用途

| 值 | 教师可见名 | 要点 |
| :--- | :--- | :--- |
| `assessment` | **常规课** | 可出题、绑导学单；可参与推进与作业均分 |
| `attendance` | **课堂考勤** | 可 0 题；签到；**不计作业分**；**永不自动/手动推进** |

- 教师「添加课程」：直接进设计器选用途，无用途弹窗。  
- 设计器文案：常规课 / 课堂考勤（不再叫「测评课」）。

### 2.2 导学单（面向教师）

- 新建：标题、年级、学期、第几课、可见范围。  
- **已去掉**：教学主题、预计完成时间。  
- 选模板：默认年级+学期，**不默认第几课**；无开发向文案（带入课程信息 / 权限内全部等）。  
- 模板库：无英文 kicker、无版本号列等冗余列。

### 2.3 课程推进（教师级统一策略）

**入口**：教师首页「课程设置」**右上角**（不在设计器、不在每节课）。

| 按钮 | 行为 |
| :--- | :--- |
| **手动一键课堂推进** | 选年级；班级**多选**且**默认全选**；按班识别当前课；达「有成绩」比例则**立刻**切下一课；可部分成功汇总提示 |
| **设置** | **一套**策略：自动推进开关、有成绩比例、等待小时；保存后 **bulk** 同步该教师本校全部常规课 |

**接口**

- `GET/PUT /business/lesson/advance-policy`  
- `POST /business/lesson/manual-advance`：`{ entryYear, classCodes: [] }`（兼容单个 `classCode`）

**规则**

1. 策略维度：教师 + 本校；常规课共用；考勤永不推进。  
2. 新建/保存常规课：自动套用统一策略（设计器无推进表单项）。  
3. 自动推进：Quartz `lessonAutoAdvanceTask.scanAndAdvance`（约 5 分钟）+ 延迟；有成绩 = `countScoredStudentsByLessonAndClass`（与 scoreReady 一致，请假不计）。  
4. 达标时间按“学校 + 入学年份 + 班级当前指派”独立记录；手动与自动推进共用行锁、条件更新和当前课唯一索引，重复点击或 Quartz 并发只能成功一次。  
5. 推进后当前课立即切换；学生仍可在推进历史记录后的 **15 分钟**内补交原课程，超过窗口、串班、串校或从未指派均拒绝。可通过 `student.submission-grace-minutes` 调整。  
6. SQL：`sql/lesson_auto_advance.sql`、`sql/lesson_assignment_current_unique_fix.sql`、`sql/typing_answer_dedup_fix.sql`、`sql/lesson_attendance_mode.sql`。

### 2.4 关键代码

| 能力 | 前端 | 后端 |
| :--- | :--- | :--- |
| 教师首页 / 推进 UI | `teacher/index.vue` | `BizLessonController` advance-policy / manual-advance |
| 策略读写 | `api/business/lesson.js` | `BizLessonServiceImpl` + Mapper bulk |
| 自动/手动推进 | — | `LessonAutoAdvanceService(Impl)` |
| 课程设计 | `lesson/designer.vue` | `saveLessonDetails` 套用策略 |
| 导学单 | `guideSheet/*`、组件与 wizard | 既有 guide-sheet API |
| 学生端 | `views/student/*` | `StudentHomeController` |

### 2.5 验收备忘

- 系统专项验收：2026-07-21 使用最新 clean package 与 Vue3 dev 服务完成 64/64 PASS。区域抽测覆盖 100 分事务回滚、7 月 20 日年级匹配、400 人名单、学生上传/交卷、匿名评卷、评卷关闭后的详情与文件权限撤销、发布统计；导学单覆盖模板创建/更新/归档、课程快照不可变和关闭后保留历史。页面无 page error/持续 500，测试业务数据和上传目录已精确清理，报告保留在本机 `output/playwright/release_feature_e2e_report.json`。  
- 构建与迁移：后端业务测试 147/147、admin 测试 2/2、clean package、导学单前端 Node 回归和 Vue3 生产构建均通过；表结构主体已在隔离的本机 schema 完整演练并通过 13 张表、发布标记、4 个启用任务和两个重复组为 0 的复核，演练库已删除；后补的幂等菜单权限脚本已在正式库成功执行并完成独立复核。
- 推进多选/统一策略：代码已在发布分支；本次服务器冒烟覆盖登录和导学单/区域抽测核心入口，手动多班推进与策略 bulk 仍按专项业务验收执行。
- 内网服务器已完成切换；正式三角色接口与页面冒烟 24/24 PASS，课堂 WebSocket 使用真实教师登录态握手成功。

### 2.6 导学单与区域抽测发布口径（2026-07-21 已确认）

1. 全平台新学年口径以**每年 7 月 20 日**为切换日；本次至少保证区域抽测前后端与现有课程核心逻辑一致。
2. 课程创建者可以查看自己课程**实际指派班级**的导学单进度、答卷、上传与成绩；模板作者身份不产生课程成绩权限，管班教师仍需精确学校、入学年份和班级授权。
3. 区域抽测每名学生的实际试卷必须严格为 100 分。随机抽题按实际抽取数量计算；同一随机题型分值必须一致，后端在保存组卷和开启抽测时双重校验。
4. 抽测普通新增/编辑请求不能写状态、评卷开关、总分和开启/关闭/发布时间；状态只能经开启、关闭、发布专用接口转换。
5. 每校只允许一个真实有效行政班，学段、年级、入学年份必须匹配；生成 0 名有效考生时禁止开启。评卷教师必须是有效教师账号。
6. 发布、评卷开关和打分对抽测主记录加行锁；只有“已关闭且评卷开启”时才能改分或读取评卷材料，发布后立即失效。
7. 匿名操作题新上传路径的所有权目前缓存于 Redis 60 分钟。正常草稿会立即持久化路径；“上传成功但草稿保存失败，随后缓存过期/Redis 重启”的低概率场景可能要求学生重新上传。本次不新增持久化表，作为已知可用性风险保留。

### 2.7 2026-07-21 内网部署记录

- 目标服务：后端 3009、Vue3/Nginx 3010；80 端口站点未修改，切换前后页面内容哈希一致。
- 发布目录：`D:\program\3009dazipingtai\releases\20260721_192346_a88cdcd7`。旧 jar、旧前端目录和旧代码全部保留，但旧后端进程已停止，避免双写。
- 迁移前整库备份：`D:\program\3009dazipingtai\backups\20260721_192346_a88cdcd7\ry-vue-before.sql`，SHA-256 为 `9926b203e3c0c2b602941d39c3292ceefa0b56e049f5bae7dc7fc8b0d09bc642`。
- 正式库复核：13/13 张新增表、14/14 个关键字段、7/7 个关键索引、发布标记、4 个启用且禁止并发的 Quartz 任务均正确；当前课程重复组和学生答案重复组均为 0。
- 导学单菜单需在 v2 表结构之后执行 `sql/guide_sheet_menu_permissions.sql`。正式库已执行并复核：7 个权限点唯一，admin/teacher/researcher 各 7 个角色关联，未重新创建旧 `biz_guide_sheet_assignment` 表。
- 后端以 NSSM 服务 `NewDaziBackend3009` 运行，启动类型 Automatic；当前 Java 进程使用发布 jar 和发布目录外置配置，启动日志无致命异常，stderr 为空。
- Nginx 3010 已切换到发布前端，`/prod-api/` 与 `/ws/` 分别代理 HTTP API 和课堂 WebSocket。服务器曾残留两个同版本 Nginx master，导致 reload 未命中真实流量；多余 master 已优雅退出，PID 文件与唯一 3010 监听者已校正。
- 服务器验收：教师导学单管理与匿名评卷、教研员区域抽测、学生首页与导学单页面全部通过，无 page error 或 5xx；真实教师 Cookie + 精确 Origin 的 `/ws/classroom/...` 握手成功。

### 2.8 学年切换故障与修复状态（2026-07-21，已修复）

- **事故事实**：真实 2024 级课程被教师首页错误归入 2025 级，真实 2025 级课程未显示；错误路由参数触发成绩权限拒绝。正式库课程、学生、教师管班和成绩数据均存在，未发现数据删除。
- **根因**：教师首页用动态当前年级匹配静态课程 `grade`；成绩、设计器、画像等又混用 7 月 20 日、8 月 15 日和 9 月 1 日边界。
- **已确认方案**：新增稳定 `biz_lesson.entry_year`，证据回填且冲突即中止；课程归届、成绩可见集、设计器和自动推进统一使用该字段；全平台统一 7 月 20 日切换。`grade` 只保留为创建时内容年级快照。
- **实现状态**：代码提交 `1d0e7ef` 已 push 至 `codex/hotfix-academic-year-20260721`。课程创建/编辑、首页分组、成绩可见集、设计器、导学单班级权限与自动推进均显式使用稳定届别；动态学年统一经 7 月 20 日边界工具计算。
- **验证状态**：后端目标测试 27/27、业务模块 157/157、admin 2/2、clean package 通过；前端 Node 边界测试 3/3、生产构建通过。本机迁移与安全重跑通过，本地 Playwright/API 28/28 PASS；真实旧 `7d3eff1` JAR 已在隔离端口实测可在字段回滚为可空后新建/删除课程。
- **关键回滚约束**：正式字段为 `NOT NULL`，旧后端 INSERT 不写该字段，**绝不能只切回旧 JAR**。发布目录中的 `rollback_academic_year_hotfix.ps1` 固定执行：停新后端 → 校验并执行 `lesson_entry_year_anchor_rollback.sql` 把字段改回可空 → NSSM/Nginx 切回旧发布 → 启动并探活旧后端。脚本已在服务器 `-ValidateOnly` 通过，数据库与旧 JAR 联合能力已在本机真实演练。
- **接力文档**：事故证据、原阻塞和完整执行顺序仍保留在 `contexts/2026-07-21学年切换故障接力.md`，仅作审计记录，不再表示当前未发布状态。

### 2.9 学年稳定锚点热修发布记录（2026-07-21）

- 线上发布目录：`D:\program\3009dazipingtai\releases\20260721_212007_1d0e7ef`；旧发布 `20260721_192346_a88cdcd7` 完整保留。后端 NSSM `NewDaziBackend3009` 为 Automatic，重启后仍使用新 JAR；Nginx 3010 指向新前端，80 端口未修改。
- 新正式库全量备份：`D:\program\3009dazipingtai\backups\20260721_212007_1d0e7ef\ry-vue-before-entry-year.sql`，57,986,966 bytes，SHA-256 `fcc2bc97b376b1d3220e794000b4e544756beee2088c7c9d5aeb623dea52c06b`。同目录保留迁移前置检查、迁移、postflight、Nginx 配置备份和日志。
- 正式迁移前置检查：非法届别证据 0、多届冲突 0；7 门无指派/历史/答案证据课程已逐条核对。4 条为无学校归属的早期孤立/样例课程，另 3 条按静态年级和 2025 基线分别锚定 2024、2023、2023；这是唯一仍需长期留意的历史数据推断风险。
- 正式迁移结果：227/227 回填成功；来源为当前指派 47、学生答案 173、人工核对 fallback 7。最终 `entry_year varchar(4) NOT NULL`，空值 0、非法年份 0、多届冲突 0、锚点与业务证据不一致 0，`(dept_id, entry_year)` 索引存在；分布为 2025=47、2024=40、2023=55、2022=27、2021=26、2020=32。
- 线上只读 API + Playwright 共 42/42 PASS：教师首页 2024 级精确为课程 25/31/33/34/38/91/161/201/217/227/237/251 并显示九年级，2025 级精确为 263～266 并显示八年级；两届成绩课程均按 `entryYear` 锚定，summary/analysis/矩阵可读，跨届组合被拒绝；教师成绩/导学单/匿名评卷、教研员区域抽测、学生首页/导学单页面无 page error 或 5xx。
- 发布后重启与 postflight：3009/3010 均为 200，服务与 Nginx 仍使用新发布，启动/SQL/字段/排序规则严重日志匹配 0。线上验收为只读，未新建正式课程；新建课程写入稳定届别、编辑禁止跨届漂移已由本地 28/28 E2E 覆盖。

---

## 三、历史交付（仍有效，摘要）

| 阶段 | 内容 |
| :--- | :--- |
| 成绩 | 分页懒加载；单课总分=作业+表现 clamp；多课均分；导出三列；请假不计均分 |
| 操作题 | previewStatus/Path；失败重试服务与 Quartz；批改页重转 |
| 答题 | 最新一条聚合；存在则更新 |
| 画像 | 学年维度（非学期） |
| 导学单 P0–P2 | 模板库/绑定、设计器、预览、提交墙、考勤空课等 |
| 区域抽测 | 清单/状态说明 |

被否决、**不实现**：卡片密度、批改入口大改、机位锁等。

---

## 四、上线前检查清单

1. 目标库完整备份并通过重复数据/索引前置检查后，从 `sql/` 目录用 MySQL 客户端执行 `release_20260721_server_upgrade.sql`；该入口已包含导学单菜单权限增量，不要重复执行已存在字段对应的非幂等脚本。
2. 后端 `mvn -pl ruoyi-admin -am clean package`；Quartz 自动推进任务启用。  
3. 前端 `npm run build:prod`。  
4. 本机导学单和区域抽测专项生命周期已通过；服务器切换后仍执行三角色冒烟，并复核手动一键课堂推进（多班）、设置 bulk、考勤不进均分/不推进。  
5. 库/Redis/上传路径/验证码；**勿把生产密码写进仓库**。

---

## 五、上线审查（Codex 短提示 · 自主判断）

把下面整段交给 Codex 即可（细节以本文件为准，勿再贴长清单）：

```text
你是本仓库上线前审查代理。先读 AGENTS.md 与 contexts/context.md，以代码与 SQL 为准。

目标：找出上线前必须修的 bug、明显体验/性能/安全问题，能修就修；不确定的标风险，不要为审查而大重构。

自主判断：
- 按影响排序：数据正确性 > 权限/安全 > 主路径可用性 > 体验 > 锦上添花
- 主路径：登录 → 教师建课/指派 → 学生答题/签到 → 交卷/批改 → 成绩与导学单 → 推进与考勤规则
- 重点核对 context 已定结论是否与代码一致（常规课/考勤、教师级推进多选立刻切课、导学单字段与文案）
- 发现高优先级问题：本机最小修复 + 必要编译/单测；低优先级只列清单
- 禁止：push、连生产写库、部署服务器、删库、改无关模块、换技术栈
- 交付：修了什么 / 怎么验 / 剩余风险（按严重度）/ 是否建议上线

默认本机。需要账号与端口见 AGENTS.md。
```

---

## 六、上线审查修复摘要（v2.28）

| 项 | 说明 |
| :--- | :--- |
| 批改 `POST /business/teacher/grading/grade` | 按 answer 归属学生+课程做本校/任教/创建人校验，禁止仅凭 answerId 改分 |
| 课堂表现 `/business/classroom-performance/*` | 读写均校验班级范围 |
| 签到名单 `checkin-roster` | 本校 + 创建人/管理员/任教班；任教教师可读（不走 assertCanManage） |
| 手动推进 | 仅创建人/管理员；单班事务经 self 代理 |
| 调试接口 | `/business/debug/*`、`/test/ad` 禁用 |
| 推进与交卷 | 教师+学校策略表；按班达标时间；行锁+条件更新+唯一索引；推进历史支持 15 分钟补交；答案原子 upsert |
| 数据权限 | 成绩/分析/矩阵/导出、画像、评分项与操作题提交均按本校任教班级和可见课程校验 |
| 文件权限 | `/profile/**` 不再直接读取；答卷/题目/区域抽测经授权预览下载；学生使用操作题专用上传接口 |
| 导学单课程权限 | 课程创建者可读实际指派班级；模板作者与跨校/未指派班级仍无成绩权限 |
| 区域抽测总分与状态 | 随机抽题按实际抽取题数固定 100 分；新增/普通编辑不能篡改状态、总分、评卷开关和状态时间 |
| 区域抽测并发与范围 | 发布/评卷行锁；条件改分；班级、年级、有效学生和评卷教师后端复核；发布后撤销评卷资源权限 |
| 学年切换 | 区域抽测按每年 7 月 20 日切换，与课程核心逻辑一致 |
| 默认配置 | Swagger、Druid 统计控制台、示例接口默认关闭；需要时仅本机显式开启 |
| 未改业务结论 | 常规课/考勤、统一推进策略、导学单新建字段等仍以第二节为准 |

---

## 七、仓库整理（v2.26）

**已删/精简（可再生成）**

- 根目录过程日志、临时脚本  
- `.playwright-cli/` 控制台与 page 快照  
- `.codex-remote-attachments/`  
- `output/playwright/` 重复脚本与超大无关截图  
- `output/stress/` 重复 stress 脚本、探针、过大 json  
- 过时接力长文：`contexts/prompt.md`、`contexts/电子导学单新对话接力提示词.md`（结论已并入本文件）

**保留**

- `p1p2_system_accept.py` + 最近 accept 截图/报告  
- 压测主脚本、`FINAL_ACCEPTANCE_REPORT.md`、`platform_accept*`  
- `contexts/电子导学单部署说明.md`（部署专题，非业务百科）  
- 业务源码、`sql/`、`uploadPath` 用户上传、`node_modules`

---

## 八、文档关系

| 文件 | 职责 |
| :--- | :--- |
| **本文件** | 业务事实、已定结论、上线短提示、整理记录 |
| **`AGENTS.md`** | 启动、账号、验收顺序、红线 |
| **`电子导学单部署说明.md`** | 导学单部署/迁移专题 |
| **`2026-07-21学年切换故障接力.md`** | 学年切换事故的原始现场、阻塞项、验收与部署审计记录 |

冲突时：**业务以本文件为准；操作以 AGENTS 为准。**
