# ADR-001：更新记录以真实发布为准

## 决策

平台更新日志不直接监听 Git push。只有 AI 完成制品发布并通过接口探活后，才写入或发布一条更新记录；管理员可在平台内修订、撤回和补录。

## 原因

同一次开发可能包含多次 push，且 push 可能没有部署、部署可能失败。直接把提交记录展示给教师会造成“代码已上线但实际不可用”的误导。Git 历史只用于初始化历史版本和辅助生成面向用户的说明。

## 后果

发布流程需要保留一条调用更新记录接口的步骤；SQL 迁移后，页面和权限才在目标环境生效。
