# 平台审计修复与验收门禁（2026-08-20）

## 本轮范围

- 已完成：代码层权限门禁、异常脱敏、IoT 口令密钥外置化、学生 Python 与协作失败状态、MQTT 输入与限流保护。
- 已完成：IoT 开启时的 Broker 连接改为异步启动，避免不可达 Broker 阻塞 Spring 主线程；默认关闭行为保持不变。
- 本机已执行：CryptPad 结构幂等迁移、测试残留软归档、课程 34 学校归属修复、`job_id=104` 任务名称修复；执行前均有备份，后检通过。正式库已完成只读前检，并已精确软归档 `sheet_id=1` 公开测试导学单、恢复 `job_id=104` 显示名称；未重启服务。其余正式库治理、EMQX/Judge0 启用、HTTPS/WSS 配置仍未执行。
- 协作 HTTP/WS 仍只用于内网临时验收，未作为正式安全结论。

## 数据治理执行门禁

| 候选问题 | 当前状态 | 执行前必须确认 | 建议治理方式 | 回滚依据 |
| --- | --- | --- | --- | --- |
| 课程无学校归属、跨学校指派 | 新增和修改已由服务端阻断；历史数据未改 | 运行只读前检、逐课程核对创建者和班级归属 | 先在隔离库形成逐条映射，经人工确认后用单事务更新；无证据的记录保持冻结 | 执行前相关表备份和逐条变更清单 |
| 历史课程创建/更新时间缺失 | 已确认候选，不能推断原始时间 | 日志、导入文件或可信业务证据 | 仅回填可证明的时间；其余标注历史缺失 | 执行前导出缺失记录和来源证据 |
| 重复学号/登录名 | 仅是候选，不等于 Bug | 学籍、登录、成绩和班级范围 | 禁止直接加全局唯一索引、删除账号或合并成绩 | 候选全量导出及人工确认单 |
| 教研活动、导学单、区域抽测测试残留 | 候选数据已知，业务影响未复核 | 逐条确认是否真实教学数据、是否仍处于开启状态 | 优先下线/归档，再确认是否物理删除 | 原记录导出、操作者和审批记录 |
| CryptPad 字段与代码不同步 | 本机已迁移并后检通过；正式库待复核 | `cryptpad_collaboration_v1_preflight.sql` 的字段与索引结果 | 正式库仍须备份后执行既有幂等迁移，再运行后检 | 本机备份：`backups/20260820_goal_cryptpad_pre/xueyeceping_server_20260729.sql`；SHA-256 `B4D50DB1C3360BBCB28C09196890CBD994217864A93AAAD573AA7A51FE1410C3` |

只读脚本：`sql/platform_data_governance_v1_preflight.sql`、`sql/cryptpad_collaboration_v1_preflight.sql`、`sql/cryptpad_collaboration_v1_postcheck.sql`。数据治理前检已在本机执行；CryptPad 结构迁移使用 `sql/cryptpad_collaboration_v1.sql`，本机后检确认字段和索引存在、缺失密钥房间为 0。

本机数据治理已执行一批可恢复软归档：在 `backups/20260820_goal_data_candidates_pre/audit_candidates.sql`（SHA-256 `1363DD4F1C4C6932289A23C4583C560480A7E3FD725C4B7EB898E050CE950E95`）之后，将标题前缀 `QARA0811CONC%` 的 5221 条教研主题、公共导学单 `测试722` 1 条标记为删除态；后检确认两类活动态记录均为 0，子记录未删除。区域抽测 `33` 仍保留开启状态，因存在 7 个班级和 270 个学生关联，必须人工确认后再处理。

## 外部链路验收门禁

| 链路 | 已验证 | 未验证，正式启用前必须完成 |
| --- | --- | --- |
| CryptPad | 配置缺失会拒绝会话；同班/当前课程房间访问服务端有边界；文档保存有版本 CAS；HTTP/WS 临时可用状态不代表正式安全 | 同班多人编辑、刷新保留、保存回平台版本递增、跨班/跨校动态攻击、200 人容量；可信 HTTPS/WSS 另行处理 |
| EMQX/MQTT | 接收器默认关闭；Topic、载荷、单 Topic/全局限频有单测；学生概览只允许当前课程 | Broker ACL、共享班级口令的组间发布限制、真机断线重连、消息积压和班级规模压测 |
| Judge0 | 浏览器不直连；提交恢复和服务失败路径有测试 | 30/100/300 并发、队列时延、超时/重试和成绩一致性 |
| LibreOffice | 队列、实例数、超时与自愈参数完成静态核对 | 当前机器内存占用、native OOM 和转换失败的真实根因及压力容量 |

## 性能执行纪律

现有 `output/stress/` 脚本可能创建业务夹具、登录、提交和清理数据。本轮的“不写库、不改数据”边界下不得运行。后续专项压测必须使用隔离库、独立账号和可清理夹具，并记录启动前后行数、错误率、P95/P99、队列深度、JVM/LibreOffice 内存和回滚结果。

## 目标模式阶段收口（2026-08-20）

- 阶段一基线、阶段二代码修复与角色冒烟、阶段三可恢复软归档及课程 34 修复、阶段四外部只读探活、阶段五研究活动查询专项、阶段六最终测试/构建/文档同步均已有证据；六阶段修复工作已收口。课程 1、区域抽测“33”、IoT 强隔离及真实外部并发属于证据或授权门禁，不得伪称已修复。
- 在线协作 HTTP/WS 按用户要求继续保留为临时验收态，不纳入 HTTPS/WSS 修复完成度。
- 课程 1 无任何可归属证据；区域抽测“33”对应 `exam_id=3` 且有 7 个班/270 名学生。两者均不得自动治理，见 `contexts/platform-goal-data-review-20260820.md`。
- Judge0 高并发、真实同班多人协作、真实 IoT 硬件和正式库迁移仍是环境门禁，不得以本机探活或历史报告替代。
- 定时任务 `job_id=104` 已完成本机备份和名称修复；正式库仍须按调用目标前检后再执行，不能只按乱码文本批量更新。

## 正式库已执行记录

- 2026-08-20：正式 `ry-vue` 前检确认 `biz_collab_room` 的 CryptPad 会话密钥缺失数为 0、活动教研压测残留为 0；因此未重复迁移或重复归档。
- 2026-08-20：在 `D:\program\3009dazipingtai\backups\20260820_goal_confirmed_audit_fixes` 备份 `sys_job.job_id=104`、`biz_guide_sheet.sheet_id=1` 及其 2 条答案后，条件更新任务显示名称和导学单删除标记。备份 SHA-256：任务 `D96E8E54DC36A7E1AEC08FFF7ECF755BE1FAE63C1CA854C2C4E619A1883A8C29`、导学单 `47A742F78FB8BF3D812B5D88679B78A8130A43E6BC325FE7AA7C7EB6309B8997`、答案 `51B0DCE43A04AC0D6D9A58DF3F8D396FD6C1726BFC04CCDD2FCEC9365F98FE04`。后检：任务状态未变、导学单 `del_flag=2`、答案数仍为 2、活动态测试导学单为 0；没有重启或发布。
- 如需撤销，使用 `sql/platform_data_governance_v1_rollback_confirmed_audit_fixes.sql`，它只影响 `job_id=104` 与 `sheet_id=1`，不改答案。
- 2026-08-20：正式库课程 34 的课程学校仍为初中部，但创建者账号、全部 4 条指派和指派人都属于小学部；备份 `biz_lesson` 与 `biz_lesson_assignment` 后，将课程学校精确改为小学部。备份目录 `D:\program\3009dazipingtai\backups\20260820_goal_lesson34_scope`，课程 SHA-256 `F1AA0E25A4E1C02D390C45BADB08C6BB9B9B7099C12BAA8220E791CCD10B1B05`、指派 SHA-256 `FF1D64FB27A4F602D75E6450C33FB5BDD99ABBBF8995E2519A9E74B164875386`；后检跨校指派为 0。回滚仅使用 `sql/platform_data_governance_v1_fix_confirmed_course34_rollback.sql` 恢复该课程学校归属。
- 2026-08-21：正式性能事件中的 IoT `groupedAt` 缺列异常已追溯为迁移前历史事件；当前表结构复核含 `grouped_at`，且现有分组快照为 1 配置/11 小组/43 学生。未因历史事件盲目改表或重启；真实重分组与硬件压测仍需隔离班级。
