# 画程：流程图操作题开发任务

## P0 决策与文档

- [x] 确认作为现有课程的流程图操作题，不建设独立作业体系。
- [x] 确认学生受限编辑、自动保存草稿和明确提交。
- [x] 确认 Vue3 原生 LogicFlow，不部署或嵌入 draw.io。
- [x] 确认版本化 JSON、结构检查＋教师确认、后续 AI 仅建议。
- [x] 形成需求、设计、任务和 ADR。

## P1 数据与后端底座

- [x] 新增幂等 SQL：题目配置、课程快照、学生草稿、正式提交四表及约束。
- [x] 新增 Java 领域对象、DTO/VO、Mapper 和 XML。
- [x] 实现 JSON 规范化与安全校验：类型、数量、大小、文字、坐标、ID、禁止字段。
- [x] 实现题目配置读取/保存和标准答案规则生成。
- [x] 在课程保存或首次作答时冻结画程题目快照。
- [x] 实现学生工作区、草稿乐观并发、正式提交和补交重开。
- [x] 提交事务写画程版本并使现有 `biz_student_answer` 主链可见。
- [x] 实现结构检查和建议分，不自动写正式成绩。
- [x] 实现教师批改读取接口和服务端课程/班级权限。

## P2 前端编辑器

- [x] 安装并锁定 `@logicflow/core` 依赖，验证 Vue3/Vite 构建。
- [x] 建立画程 schema、默认权限和 LogicFlow 数据桥接。
- [x] 实现四类节点、带箭头连线、判断分支文字和大按钮工具栏。
- [x] 开始/结束拆为独立入口，节点同时支持单击添加与拖到画布指定位置。
- [x] 选中节点或连线后提供可见删除按钮，并继续服从学生权限与元素锁定。
- [x] 修正输入/输出多边形坐标系，保证节点文字位于图形正中央。
- [x] 覆盖输入/输出节点默认锚点，固定为上、右、下、左四条边中点。
- [x] 实现题目级能力开关、元素锁定、教师双画布和规则编辑。
- [x] 学生基础图支持从标准答案一键复制，覆盖已有内容前二次确认。
- [x] 实现学生 2 秒防抖自动保存、本地备份、冲突提示和明确提交。
- [x] 实现只读渲染、PNG 导出和移动端查看降级。

## P3 业务页面接入

- [x] 题库页新增 `FLOWCHART` 作答方式和画程配置入口。
- [x] 课程设计器识别并展示画程流程图题。
- [x] 学生首页将流程图题切换为画程作答，不显示文件上传。
- [x] 教师连续批改页接入学生图、标准答案、结构检查和建议分。
- [x] 保留现有文件作品、Python、AI 费用和课程默认开放等既有行为。

## P4 测试与验收

- [x] Java 单测覆盖文档校验、文字规范化、规则生成、规则悬空引用、拓扑检查和建议分。
- [ ] 在数据库闭环验收中覆盖草稿并发、重复提交幂等和学生/教师越权。
- [ ] 前端单测覆盖 schema、权限和本地草稿。
- [x] 画程定向单测通过（7/7，含规则生成接口返回语义）。
- [x] `mvn -pl ruoyi-admin -am clean package` 通过（业务 380 + admin 3）。
- [x] `npm run build:prod` 通过。
- [x] 本机执行 SQL 前完成整库备份，并完成四表、唯一键和空数据后检。
- [x] 浏览器完成教师→学生→教师主链、自动保存、小屏只读和提交锁定冒烟（15/15）；断网恢复与双页面冲突仍归入并发专项。
- [x] 教师设计器交互专项通过（本机 13/13、正式 14/14）：开始/结束、单击/拖拽添加、节点/连线删除、文字居中、四边中点锚点和复制基础图，页面错误与 HTTP 500 均为 0。
- [ ] 400 学生自动保存容量专项在功能闭环稳定后执行。

## P5 文档与发布

- [x] 更新 `contexts/PROJECT_CORE.md` 当前事实、验证、SQL、风险和下一步。
- [x] 更新 `docs/architecture/INDEX.md`、`BUSINESS_BOUNDARIES.md`、`DATA_MODEL.md` 和必要架构图。
- [x] 正式环境已完成整库备份、上传新 release、SQL、探活、浏览器验收和回滚登记（v1.27.0）。
- [x] 已更新 `contexts/RELEASE_LOG.md`，正式库 `biz_platform_update` 的 1.27.0 为唯一 `PUBLISHED` 记录。
