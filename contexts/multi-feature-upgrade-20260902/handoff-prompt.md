# 新任务交接提示词

下面内容可以完整复制到新的 Codex 任务中：

```text
请接管 D:\dmwprogram\newdazipingtai\newdazi 的“2026-09-02 多功能改造计划”。默认使用简体中文。先使用项目 project-context，严格按以下顺序阅读：

1. AGENTS.md
2. contexts/PROJECT_CORE.md
3. docs/architecture/INDEX.md
4. contexts/multi-feature-upgrade-20260902/requirements.md
5. contexts/multi-feature-upgrade-20260902/design.md
6. contexts/multi-feature-upgrade-20260902/tasks.md
7. contexts/multi-feature-upgrade-20260902/development-prompts.md
8. 你准备实施阶段对应的专题 requirements/design/tasks/ADR

首先运行 git status --short。本仓库已有大量用户/其他任务未提交修改，严禁覆盖、回退或顺手整理不相关文件。总方案文档已经完成，但截至文档记录时尚未开始这些需求的业务代码、SQL或发布；不要把“文档已写”误认为“功能已实现”。

已确认业务决定：

- 共享课程仍只给负责课程指派班级的同校教师；共享教师可只读查看课程内全部公有/私有题，但不能查看未加入课程的私人题库，也不能复制、设计、删除或修改。
- 操作题数字评分保持默认；五星是辅助，支持整题和逐评分项切换；按 round(maxScore×stars/5) 四舍五入保存整数，7分题为1/3/4/6/7；零分有清零按钮。
- 旧作品永久使用提交时评分快照，新提交版本使用当时最新快照；已有提交后禁止修改该题总分。
- 作业状态采用事务后 WebSocket 推送加定期全量校准；教师首页、成绩/提交列表、批改页和学生桌面统一接入。打开题目=已进入，首次保存=作答中，正式提交=已提交待批。
- 新建课程默认不选班级；从具体班级入口只预选该班；编辑只回显已保存班级。
- 流程图开始/结束为胶囊，处理和输入输出框不圆角，连接锚点扩大。
- 班级可以保存多套分组方案，支持平均、学号区间、手动拖动；每人恰好一组；每组一名可调整的视觉组长；课程使用课时快照。不能复用IoT实验分组表。
- 学生桌面不是点名/考勤。它是机房终端在线网格：姓名、学号、在线/离线、连接IP、多设备、组别、视觉组长和轨迹入口；现有签到考勤课不变。
- 学生桌面主入口放在“班级管理”已管理班级行内，教师首页提供当前课程/班级快捷入口；不新增一级菜单，不放教师工具或在线协作下面。
- 在线由学生登录后的独立认证Presence WebSocket表示，30秒心跳、60秒离线，当前状态存Redis TTL；IP由服务端从可信代理链观察，称“连接IP”。浏览器不能保证取得真实网卡IP或计算机名；NAT下可以显示共同网关IP。
- 在线协作改为独立非计分课堂协作活动，不占操作题、不设置分数、不写个人答案或课程推进。一个活动可有多个起始文件任务版本，小组默认顺序映射并允许调整；模板可复用但每组文档副本隔离。
- 首名学生进入后冻结协作任务和映射；课中换组需教师确认和审计，学生失去旧组入口、获得新组入口，旧文档和轨迹保留。
- 现有全班共同文档保留为全班组历史模式，不能自动拆分或删除。
- 操作轨迹记录进出、心跳、保存和相邻revision的文档变化。版本差异属于整个小组文档；只能说“某学生客户端触发保存，小组文档发生变化”，不能把全部变化认定为该学生个人贡献。

已核查的正式环境证据：

1. 最近24小时严重/异常事件已经全部归类：
   - /business/question 有5条真实缺陷，更新时把非空 practical_image_max_count 写NULL。
   - 8条严重记录是学生删除保护或班级范围校验等预期业务拒绝误报。
   - /business/question/importData 有4条缺 question_type 错误，当前缺少写前校验、可能部分写入且会泄露SQL细节。
   - 学生导入有10条约3.3～3.7秒历史慢事件，需验证线上JAR动态分级。
   - NSSM活动运行目录与stdout/stderr日志目录不一致，后续发布应改为稳定日志目录。
2. 郑琦实际活跃账号为696260。课程293、题目2029、满分70：旧快照v1是3项30/35/35，绑定202个提交版本、172条答案；新快照v2是2项50/50，尚未绑定提交。根因是 RuoYi-Vue3/src/api/business/scoringItem.js 没有把 practicalVersionId 放进请求参数，后端快照机制和历史数据本身存在，不需要覆盖数据。
3. 当前 ClassroomWebSocketHandshakeInterceptor 不提取IP，Handler只保存进程内连接；现有IpUtils能读取代理头，历史日志出现过真实10.52.*地址。学生桌面需要新增Presence状态底座，不能仅靠当前课堂控制WebSocket。
4. 当前协作房间唯一键没有小组维度，当前只能一班一房间；现有revision只记录保存版本和保存触发用户，不能证明字符作者。
5. 项目已依赖Apache POI的poi-ooxml和poi-scratchpad，可用于Office版本结构化差异PoC；复杂格式必须降级，差异任务不得阻塞保存。

开发优先级固定为：

P0-A：郑琦评分快照参数 + 题目NULL；
P0-B：题库导入、诊断分级、日志路径；
P1-A：指派默认 + 流程图；
P1-B：五星评分；
P1-C：共享课程只读；
P1-D：作业实时状态；
P2：通用分组 + 学生桌面；
P3-PoC：Office版本差异和作者数据能力；
P3：分组多文档协作和轨迹。

请先告诉我你准备执行哪个阶段；如果用户没有指定，推荐从 P0-A 开始。一次只实施一个阶段。修改前说明文件、原因、预期、风险；涉及SQL、WebSocket架构或新依赖时先给2～3方案并等待确认。按编译/单测→接口→必要浏览器冒烟顺序验收。未经明确要求不要发布正式服务器；正式发布必须备份、SHA-256、新release、回滚点、RELEASE_LOG和biz_platform_update登记。每个重大阶段完成前同步PROJECT_CORE、架构资料和对应专题文档。
```
