# Python 在线编程与 Judge0 CE 需求

## 目标

在既有题库、课程、学生答题和成绩体系中增加 Python 在线编程与自动判题。Python 是“操作题”的作答方式，不是第四类题型或成绩；浏览器不得直连 Judge0，第一阶段只支持 Python。

## 验收要求

- 教师在“操作题”中选择“Python 在线编程”，配置初始代码、公开/隐藏测试点、分值权重与资源限制。
- 学生可保存草稿、运行公开示例、正式提交、查看本人与本题的历史。
- 后端鉴权后调用 Judge0，异步轮询并将正式终态写回既有 `biz_student_answer`。
- 状态必须包含等待、判题中、通过、部分通过、答案错误、语法错误、运行错误、超时、内存超限、服务异常和取消。
- Judge0 故障不丢失代码或提交，不写零分。
- Judge0 仅允许主平台后端访问；学生代码无网络、CPU/内存/进程/输出/文件均受限。
- 同一门课程可同时配置文件作品操作题和 Python 在线编程题；两者均属于操作题，课程总分仍由全部题目分值合计为 100 分。
- 成绩查询默认保留“操作题”合计，并在课程详情显示文件作品与 Python 编程两个小计，不新增第四类成绩。
- 学生课程题目 DTO 缺失作答方式时，页面只能向平台后端核验该题是否为已启用的 Python 题；核验失败必须继续按文件作品处理，绝不由浏览器直连 Judge0 或猜测为编程题。
- 学生 Python 题应清晰展示输入说明、输出说明、公开样例输入、期望输出、运行实际输出、错误信息、状态与历史提交；隐藏测试点只能返回汇总结果，不返回隐藏输入和答案。
- 学生编辑器至少支持 Python 高亮、行号、自动缩进、括号匹配、撤销重做、查找替换、折叠、关键词补全、主题切换、全屏和移动端布局；编辑内容不得因轮询刷新丢失。
- 草稿保存必须防抖，保存期间的新输入必须补发最新版本；判题轮询达到上限后显示可恢复状态，不得永久停留在“判题中”。

## 不做

- 第一阶段不支持 Python 以外语言、交互式输入、多人协作代码编辑或公开 Judge0 API。
- 不新建用户、课程、班级、总成绩或替代现有权限模型。

## 独立 Python 刷题（已上线）

### 产品目标

在既有课程 Python 操作题之外，提供面向日常练习的简化 OJ。它使用已有题库、CodeMirror 和 Judge0，但练习记录、提交记录和统计与课程作答、课程推进、课程总分完全隔离。

### 教师配置规则

- 教师端只有一个一级菜单“Python 刷题”，只保留一种“练习题单”，不再区分年级基础题单和班级加练包。
- 每个练习题单选择一个或多个教师可管理班级；“全年级”只是一次选中该年级全部可管理班级的快捷操作。
- 同一班级可以接收多个练习题单；需要额外练习时新建普通题单，不引入第二种加练对象。
- 选题工作台参考现有课程设计器的左右双栏：一侧显示题单设置和已选题目，另一侧显示 Python 题库；必须增加知识点/难度/验证状态筛选、预览、多选批量加入、排序和移除。
- 练习题单支持修改和删除。修改已发布题单时复制为新草稿版本，旧发布版本继续服务学生，直到新版本再次发布；教师确认删除后，无论是否已有练习记录，都物理删除题单、版本、班级关联、题目快照、草稿、提交、逐点结果和进度，不提供“已删除题单”或恢复入口。

### 刷题验收要求

- 新手教师可选择目标班级后直接使用系统推荐题单，也可从题库批量选题。
- 教师可按题单、班级、学生和题目查看练习情况，不再拆分基础题与加练题统计。
- 学生只可访问发布给本人班级的练习题单，不可访问其他班级题目或记录。
- 每题首次或再次判题均复用既有 Judge0 能力；刷题提交不写入 `biz_student_answer`，不改变课程成绩。

## V1 历史实现状态（2026-08-19，已由 2026-08-22 统一题单替代）

- 已完成本地教师工作台、基础题单版本发布/撤回、班级加练包草稿/发布/撤回和基础学情查询；页面入口为教师“Python 刷题”。
- 2026-08-19 体验修正：教师创建题单只输入年级和题单名称；服务端按学段自动推算入学年份并固定默认学期。题库管理可按 Python 在线编程/文件作品筛选，配置题目与班级学情均使用可视化选择，不允许手输题目 ID 或班号。
- 已完成学生独立题单、基础题/班级加练分区、全部/未完成/已完成/错题筛选、CodeMirror 6 编辑、草稿、公开样例运行、正式提交、异步结果轮询和历史记录；入口为学生“Python 练习”。
- 服务端已按学生学校、入学届别、班级重新计算题目范围，并校验教师可管理班级；刷题提交和进度只写独立表，不写课程答案或成绩。学生控制器使用既有 `studentSs.isStudent()` 业务鉴权，兼容未维护系统角色表的本机学生账号。
- 已完成发布前题目快照及公开/隐藏测试点校验，并补齐加练包与基础题、同班已发布加练包之间的重复题阻断。
- 已完成开发库和正式库迁移、80 道系统题导入和参考解法 160/160 验证；正式整库备份、release 切换和 3009/3010 探活均完成。真实 Judge0 单题提交为 `ACCEPTED / 100 / 2/2`，追加 3 个并发提交全部通过；正式教师/学生浏览器冒烟无 page error。验收题单、草稿、提交、进度已精确清理，`biz_student_answer` 保持 0。高并发和班级规模压测仍未执行。

## 2026-08-22 已确认：OJ 化改造需求

- 统一题库继续复用 `practical/PYTHON`，不增加适用场景字段；课程或刷题用途由关联关系决定。
- Python 编程题管理不显示、不要求年级、学期和第几课，显示标题、知识点、难度、测试点和验证状态。
- 学生端采用左题面、右上代码、右下控制台的可拖动三窗格；支持公开样例、自定义输入、正式提交和脱敏逐测试点结果。
- 第一阶段仅支持 Python 3 ACM 输入输出模式。输出比较统一换行、忽略行尾空白和文末空行，其他内容严格比较。
- 教师使用三步极简出题和双 Sheet Excel 导入；参考代码必须在 Judge0 通过全部测试点后才能发布。
- 教师端不接 AI 出题。系统题 V2 首版 120 道，由网页端 AI 分批起草，Codex 自动校验、修订、导入。
- 旧 80 道系统题按教学价值重写、合并或删除；用户明确不保留 V1 题单和提交历史，正式清理仍需先做精确依赖前检和整库备份。

### 2026-08-22 正式实现状态

- P1～P5 及本轮收口已发布到 `10.52.1.123`：当前 release `20260822_181500_python_practice_polish_v3`，3009/3010 均为 HTTP 200。
- 练习题单已统一为版本绑定多个班级，不再使用基础题单/班级加练双模型；教师端完成课程设计器式选题、批量加入、预览、排序、发布、物理删除和统一学情，不显示“已删除题单”，不提供恢复。
- 学生端按题单分组，保留左题面、右上代码、右下控制台的三窗格；自定义运行、公开样例、正式提交、逐测试点结果与历史均走独立刷题数据，不写课程成绩。
- 系统推荐题单只选公开、启用、已验证且编程配置来源标记为 `python-system-v2` 的 V2 系统题，并按难度比例和知识点多样性生成；题目表的创建人统一显示“郑东旭”，系统来源与人员署名不再复用同一字段。
- Python 题保存时必须清空年级、学期和课次；修改题面、配置或测试点后验证状态必须回到 `DRAFT`。
- 只有 `enabled=1` 且 `validation_status=VALID` 的 Python 题允许加入课程或练习题单。
- 自定义运行只保存本次自定义输入和公开运行结果，不计分、不更新刷题进度；隐藏测试点只暴露状态、耗时和内存，不暴露输入、期望输出、实际输出和错误详情。
- 双 Sheet 每批最多 30 题，必须先预检并跑通参考代码，确认令牌 30 分钟有效且不能重复确认；整批在一个事务中导入。
- 系统题 V2 已生成并导入：`PYV2-001`～`PYV2-120` 共 120 题、720 点，真实生产 Judge0 720/720 通过，正式库双跑导入后仍为 120 个唯一题号、240 个公开点、480 个隐藏点、疑似乱码 0。
- Judge0 中文链路统一使用 UTF-8 Base64 请求/响应，并兼容响应 Base64 末尾换行；相关单测和真实中文固定输出题均通过。
- 旧 80 道 V1 题及其课程/刷题依赖、历史提交和快照已在整库备份后物理删除，正式后检 V1 残留为 0；当前 V2 为 120 题、720 个测试点。
- 所有 Python 题在统一题目表中的年级、学期和课次均为空，题库列表和编辑表单不显示这些课程属性；正式后检异常元数据为 0，创建人异常为 0。
- `entry_year` 表示入学年份，语义为“2024级”而不是“2024届”。选择班级、题单列表和学情页面均以 7 月 20 日为学年切换点，结合学校学段换算当前自然年级，并显示为“801班、901班、1201班”等；未来班级和已毕业班级不出现在可选列表。
