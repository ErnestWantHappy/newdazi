# 2026-09-07 502 与登录会话只读复核

核验时间：2026-09-07 15:16 起（北京时间）。本轮通过 SSH 只读读取注册表、配置、活动 JAR 内配置、Windows 事件、Nginx/后端日志，并执行登录日志 SELECT（只读事务）。未改服务器文件、配置、服务或业务数据，未重启、发布或生成新登录会话。仅修改本地交接文档。

## 当前发布状态

- NSSM AppDirectory：`20260907_flowchart_ai_fix_v1`；3010 root：`20260907_flowchart_ai_ui_v2/frontend`。两项服务 Running。
- 1.30.8 quickfix 当前未运行；数据库平台更新版本记录本轮未复核，不能据其判断活动制品。
- JVM 参数 `-Xms512m -Xmx2048m`；活动 JAR 的 application.yml 为 `expireTime: 120`，检查的外置 YAML 未见同名覆盖；环境变量覆盖本轮未完整核对。
- Nginx 配置 `worker_processes 1`、`worker_connections 1024`；同时承载80、3010及其他入口，access.log为共享日志，不能把日志总行数直接等同于平台独立请求数或学生数。

## 502 时间与原因

共享 access.log 严格区间 `[14:00:00,14:50:00)`：88649行，200=80041、304=4817、502=2259、101=1260、499=108、400=157、405=7，未见504。

- 14:17=1116条502，14:18=1055条502，合计2171/2259=96.1%；余88条散布14:07至14:32。
- 后端服务13:48:47停止、13:49:36运行；14:17:43停止、14:18:24运行。
- 后端应用日志14:18:24.817开始启动，14:18:39.359完成启动。服务Running并非业务就绪，不能将不可用时间固定为41秒。
- Nginx服务13:50:17停止、13:50:33运行；14:18:42停止、14:18:57运行。
- error.log中14:17/14:18分别863/833条10061拒绝连接指向127.0.0.1:3009，支持重启窗口是集中502的主要直接原因。错误日志与共享访问日志并非一一对应，不能称2171次均为直接连接3009失败。
- 其他88条错误为读取127.0.0.1:3010响应头时10054连接重置，涉及学生当前课、协作当前/历史、抽测检查、session-status等；具体导致重置的原因尚未确定，不能将全部卡顿和502归为冷重启。
- RDP事件14:15:53为Administrator从10.52.3.60重新连接既有会话，14:18:29断开；仅凭时间相邻不能确认是谁执行重启。旧交接自述发布/回滚可作线索，不能据此宣称服务器整机重启。

## 登录日志与人数口径

固定区间 `[2026-09-07 00:00:00,15:00:00)`：12125次尝试、9099次成功、3026次失败；全部尝试去重用户名2270、成功登录去重用户名1905、日志去重IP58。未按角色筛学生，不能称1905名学生或同时在线1905人。

14:00–14:49失败原因：用户名不存在/密码错误1270次，连续密码错误导致锁定94次。14:14成功596次但成功去重账号仅60个；14:19成功534次但去重账号仅34个。重复登录早于14:17回滚，无法全部归因为重启后刷新，需结合机房登录方式与浏览器行为继续定位。

本地代码：在线列表枚举login_tokens并逐个读取LoginUser，诊断中心统计key数量；createToken每次生成UUID，verifyToken剩余不足20分钟可续期。因此有效会话数不等于人数，也不严格等于过去120分钟登录次数。用户确认7660出现在14:30–15:00在线列表；本轮未全量读取生产Redis会话，未复现7660快照，不能精确分解该数。

共享IP不能识别独立学生或机房；交换机本身不意味着NAT，同IP可能来自出口转换或代理链。不能因同IP定性攻击，也不能据此排除登录循环或自动重试。

## JVM与LibreOffice

- Application Error事件1000确认soffice.bin在14:00:02、14:00:07、14:13:13和14:35:39–14:36:16多次崩溃；1001为错误报告，不能每条计一次新崩溃。
- 后端日志确认LibreOffice连接丢失后自动重启，14:35–14:36多次拉起。文档转换存在异常；尚未证明耗尽CPU或导致全部页面卡顿。
- 未取得事发时GC停顿、堆曲线、CPU、线程或连接池饱和证据。2GB堆与1024连接配置只是容量线索，不能直接判定过小/打满；全量在线列表有开销，但未测定本次分配量或Redis阻塞时长。

## 三处本地修复核对与下一步

- 用户补充：机房学生均手动输入登录；暂不决定在线人数新口径，优先故障调查。手动输入不代表日志中的重复登录已解释，仍需核对是否登录后退回、连续回车或跨标签操作。
- 本地登录代码存在重复提交候选：密码框回车调用handleLogin，该函数入口无loading防重入；/login显式repeatSubmit:false绕过公共重复提交拦截。按钮loading不能代替函数防重入。尚未对活动前端制品和现场连续回车做复现，不作为已确认事故根因。当前会话处理代码未将普通502直接作为401，也未发现自动重新提交登录逻辑。

- ClassroomPerformanceController当前diff仅删除/save、/batch-save原因必填校验。
- classroomDesktop已不拦截空原因；student首页当前/历史协作入口已做roomId判空，未发现此前单房间自动replace逻辑。
- 本轮未构建、测试或发布。其他工作区改动保留，不因文件未提交就认定线上未发布；协作迁移状态必须另做正式只读核验，旧交接与核心文档有矛盾。
- 下一步优先查重启前登录循环及3010连接重置；补充JVM指标名称、首次卡顿时间和机房登录方式。未授权任何生产写入，亦未决定登录互踢、缩短TTL或调整堆内存。
