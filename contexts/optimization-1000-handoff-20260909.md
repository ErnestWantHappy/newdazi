# 交接提示词：1000 人并发与功能/界面优化（只出方案，确认后再改）

把下面「从这里复制到新 AI」整段原样发给新的代理。不要改写后再发，以免漏掉红线和已有容量证据。

---

## 从这里复制到新 AI

你是郑东旭的长期开发代理，接手信息科技学业测评平台（RuoYi 后端 + **只维护 Vue3**）。  
**本轮目标不是立刻改代码，而是做一次有证据的优化审查，并给出可执行方案，等用户确认后再动手。**

用户原话：看看有没有可优化的地方，尤其注意并发，确认 1000 人同时可用；功能逻辑和界面也看；务必给方案，等确认再执行。

与用户交流用简体中文。大白话汇报：做了什么 / 成败 / 问题 / 下一步。技术名词第一次出现用中文解释。

### 0. 先读这些（按顺序，不要全仓乱扫）

1. 仓库根目录 `AGENTS.md`
2. `contexts/PROJECT_CORE.md`（**以文件顶部日期最新的一节为准**，当前应是 v3.52 / 2026-09-09 21:05）
3. `docs/architecture/INDEX.md`
4. `docs/architecture/DEPLOYMENT_RUNBOOK.md`、`INTEGRATIONS.md`、`SYSTEM_ARCHITECTURE.md`、`BUSINESS_BOUNDARIES.md`
5. 并发相关：`output/stress/` 里已有报告；`contexts/python-judge0/adr/ADR-011-queued-400-submissions-on-shared-host.md`；`contexts/class-grouping-and-desktop/ADR-001-server-observed-presence-and-class-entry.md`；`contexts/online-recovery-20260907/`
6. 本文件

读完后运行 `git status --short`。工作区有大量未收口改动，**全部保留**；禁止 `reset`、覆盖、把未收口改动打进 130 制品。凭据只在 `contexts/secrets.local.md`，禁止回显、禁止提交。

### 1. 当前真实运行拓扑（不要当成还在 123 上课）

- 学业测评入口：`http://10.52.1.130/`（Linux Ubuntu，16 核 / 32GB）。123 的测评后端 `NewDaziBackend3009` **已停**；`10.52.1.123:3010` 只是导航页。
- 协作大门：`http://10.52.1.130:3018`（主站）和 `:3019`（沙箱），Nginx 反代 129 CryptPad。旧协作文档已按用户决定作废。
- Python 判题：130 后端 → `10.52.1.129:2358` Judge0（10 worker，平台排队容量曾按 400 验收，不是 400 个沙箱）。
- 物联网：实验板仍连 `10.52.1.129:1883`；**平台 MQTT 接收器在 130**，订阅 `county/#`。不要把 EMQX 迁到 130。
- 域名 `xxkj.xsedu.net.cn` 仍解析到 123，登录会失败。DNS 只有教科研能改。不要自行切域名、不要停写、不要迁库。
- 130 的 quartz 副作用任务仍关，等切域名观察后再开。
- 课堂表现分前端修复只在本机 `RuoYi-Vue3/src/views/business/classroomDesktop/index.vue`，未发 130。
- 130 Nginx：`worker_processes auto`（16）× `worker_connections 4096`，`worker_rlimit_nofile 65535`（已从错误的 `events{}` 挪到主段）。Linux epoll **没有** Windows `FD_SETSIZE=1024` 硬顶。JVM 仍是 `-Xms512m -Xmx2048m`。
- 跳板：130/129 SSH 只能经 123。用仓库 `scripts/deploy_transport.py` 的指纹，或 Git 的 `ssh.exe`。Windows 自带 OpenSSH 远程 exec 可能挂死。

### 2. 本轮硬纪律

1. **先方案、等确认、再改代码或服务器。** 未收到用户明确「按某方案做」之前，禁止改业务代码、禁止发布 130、禁止改 129/123 生产、禁止压测打正式库学生成绩。
2. 方案级选择（换栈、大改库表、大重构、加依赖、改核心配置、改容量口径）必须给 **2～3 个方案**，写清思想/优点/缺点/推荐理由。
3. 「1000 人同时可用」**尚未被任何现场压测证明**。禁止用理论连接数、单测或 400 人旧报告直接宣称 1000 可行。
4. 123 只读。写 123 必须先备份+SHA。本轮优化审查默认 **不要动 123**。
5. 不为优化去改权限模型、成绩口径、一课一道操作题、协作与固定组隔离等已定业务，除非方案里单列并等确认。
6. 能本机复现的编译/单测优先本机；只有菜单/数据/线上容量必须以 130 为准时再 SSH。
7. 回复里不要密码、Token、Cookie。

### 3. 已有容量证据（审查时必须引用，不要当没做过）

这些是历史事实，场景是 **当时的 123 Windows + 129**，不能直接等于今天的 130 Linux：

| 证据 | 口径 | 结论边界 |
| --- | --- | --- |
| 2026-08-21 平台阶梯压测 | 20/50/100/200/**400** 活跃并发，13653 请求 100% 成功，400 人 3200 笔答案零丢单 | 400 档 P95 约 3.5s；**不是 1000** |
| Judge0 | 400 份提交进入平台队列、10 路真实判题；400 档接口 P95≈1.01s，约 22s 排空 | 400 是排队不是 400 沙箱；129 还同时跑 CryptPad+EMQX |
| CryptPad | 单房 50 人；400 人×8 房间会话 100%，均时延 85.9ms | 当时经 123 入口；现改 130:3018/3019，**未复测** |
| 2026-09-08 123 Nginx | Windows `FD_SETSIZE=1024`，约 400+ 人且人人 WebSocket 会顶满 | 文档写明：**1000 人需要 Linux 网关 + 300→1000 阶梯压测后才能承诺**。平台已迁 130，这条硬顶对测评主站已解除，**但 1000 仍未测** |
| 2026-09-07 | 成功登录去重约 1905（当天累计，不是同时在线）；会话条数曾上万 | 在线统计/重复登录修复有本地候选，**未作为当前 130 制品的已发布结论** |
| Presence / 课堂 | Redis SCAN 替代 KEYS；学生 WS 指数退避；课堂 10 秒校准；协作心跳不写轨迹 | 本地代码层纠偏，不等于 1000 人课堂大屏已验收 |
| 学生首页 | 取消进页对每题 `markQuestionEntered` 风暴：仅本地未发 130 | 旧前端仍可能打爆 API |

130 上 JVM 仍 2GB、Judge0 仍 10 worker、CryptPad 仍 129 共享机。这些是 1000 人的真实瓶颈候选，不要只看 Nginx 连接数。

### 4. 你要交付的东西（本轮全部是文档/方案，不是补丁）

做完只读体检后，写一份优化审查报告（建议路径 `contexts/optimization-1000-20260909/` 下的 `requirements.md` 草案 + 给用户的方案正文），必须包含：

#### 4.1 先定义「1000 人同时可用」

不要假装用户已经定义清楚。你要在方案里给出 2～3 种口径，并推荐一个作为验收标准，例如：

- A. 全县 1000 名学生在 10 分钟窗口内登录并进入当前课（峰值登录 + 首页）
- B. 1000 人同时在线作答客观题并交卷（含幂等）
- C. 1000 人同时持有 Presence/课堂 WebSocket，外加教师大屏
- D. 1000 人同时开协作文档（极不可能，也与「每组一份文档」产品不符）

推荐应贴近真实县域上课：多校同时上课，不是 1000 人挤一个 CryptPad 房间。写清每种口径会打到 Nginx / JVM / MySQL / Redis / WS / Judge0 / CryptPad / EMQX 的哪一段。

#### 4.2 并发与容量（最高优先级）

只读核对 130/129 实际配置（连接池、线程池、Redis、MySQL `max_connections`、Nginx、JVM、Judge0 COUNT、EMQX、CryptPad worker），列出：

- 已验证上限
- 理论瓶颈（带计算公式和假设）
- 未验证缺口

重点查（有代码再下结论，禁止空口）：

- 学生首页轮询、进页上报、协作 current/history 静默请求
- Presence WebSocket 数量、心跳、断线重连
- 课堂大屏 10 秒全班汇总 SQL
- 登录接口防抖与会话膨胀
- 交卷唯一键 / 幂等
- LibreOffice 转换池
- 130 反代 129:3018 的 WS 连接是否会在 1000 人协作时打满 129
- 前端大 chunk、重复请求、未发 130 的减负补丁

阶梯压测方案只能写计划：**先 300，再 600，再 1000**；用隔离账号或可清理夹具；正式库成绩表禁止当压测垫。压测脚本可设计，**未确认不得对 130 正式库开压**。

#### 4.3 功能逻辑

从教师上课路径和学生上课路径各走一遍（读代码 + 现有文档，不要编造页面）：

教师：登录 → 首页课程卡 → 设计器 → 指派 → 课堂大屏 → 批改 → 成绩 → 协作工作台 → 物联  
学生：登录 → 首页任务 → 客观题/打字/Python/流程图/文件 → 交卷 → 协作 → 物联  

找出：重复入口、文案与后端不一致、未发 130 的本地修复、死逻辑、越权风险、空状态差。每条写：证据文件/行号、影响、是否 1000 人相关、推荐改法（最小可用）。

已知待办不要装成新发现：课堂表现分前端未发 130；流程图 AI 批改本地候选未发布；quartz 副作用任务仍关；协作双人真实打开未验收；域名未切。

#### 4.4 界面

只提能改变上课效率或减少误操作的点（教师是信息科技老师，要能演示、能交付，不炫技）。例如信息过载、按钮找不到、小屏、课堂大屏可读性、学生低版本 Chrome 协作门禁。不要提议换 UI 框架。

### 5. 方案怎么写（给用户看的那一页）

按 P0 / P1 / P2 排序，每一项：

- 问题（一句话）
- 证据
- 方案 2～3 选 1（若只是小修补可只给最小改动计划）
- 工作量与风险
- 是否需要压测 / SQL / 重启 / 发 130 制品
- 不做什么

最后给出「建议先做的 3 件事」，必须能在确认后分步落地。不要一次甩 20 个并行改造。

推荐优先级暗示（审查后用证据调整，不要盲从）：

1. 把「1000 人」定义成可测口径，并做 130 只读基线（连接池/WS/JVM）
2. 清点未发 130 的前端减负（首页风暴、协作轮询）是否仍是高峰元凶
3. 再谈课堂大屏 SQL、登录会话、Judge0/协作在 1000 口径下够不够

### 6. 明确不要做

- 不要为了 1000 人把 CryptPad/Judge0/EMQX 搬到 130
- 不要对学生机开放 129 的 2358/18083，不要把 130 的 3009/3306/6379/22 暴露给学校
- 不要开启 130 quartz 副作用任务
- 不要 `git push`、不要删库、不要把工作区其他功能打进同一个优化包
- 不要用 Playwright 替代容量结论
- 不要在审查阶段改代码「顺便优化」

### 7. 验收本轮审查是否合格

用户看到报告后应能直接回答：

1. 现在能不能对全县说「1000 人同时上课」？能 / 不能 / 要先测哪一档  
2. 若要承诺 1000，最短路径是哪 3 步  
3. 功能和界面哪些值得改、哪些是口味问题可以不做  

报告写完就停，等用户点名方案编号后再执行。

---

（提示词结束）
