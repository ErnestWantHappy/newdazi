<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.CountyExamPaperQuestionMapper">

    <select id="countByExamAndStudent" resultType="int">
        select count(1)
        from biz_county_exam_paper_question
        where exam_id = #{examId}
          and student_id = #{studentId}
    </select>

    <insert id="batchInsert">
        insert into biz_county_exam_paper_question
        (exam_id, student_id, question_id, question_type, question_content, option_a, option_b, option_c, option_d,
         answer, analysis, question_score, order_num, typing_duration, word_count, file_path, preview_path)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.examId}, #{item.studentId}, #{item.questionId}, #{item.questionType}, #{item.questionContent},
             #{item.optionA}, #{item.optionB}, #{item.optionC}, #{item.optionD}, #{item.answer}, #{item.analysis},
             #{item.questionScore}, #{item.orderNum}, #{item.typingDuration}, #{item.wordCount}, #{item.filePath}, #{item.previewPath})
        </foreach>
    </insert>

    <resultMap id="CountyExamPaperQuestionDetailResult" type="com.ruoyi.business.domain.vo.BizLessonQuestionDetailVo">
        <result property="questionId" column="question_id"/>
        <result property="questionScore" column="question_score"/>
        <result property="orderNum" column="order_num"/>
        <result property="questionContent" column="question_content"/>
        <result property="questionType" column="question_type"/>
        <result property="optionA" column="option_a"/>
        <result property="optionB" column="option_b"/>
        <result property="optionC" column="option_c"/>
        <result property="optionD" column="option_d"/>
        <result property="answer" column="answer"/>
        <result property="analysis" column="analysis"/>
        <result property="typingDuration" column="typing_duration"/>
        <result property="wordCount" column="word_count"/>
        <result property="previewPath" column="preview_path"/>
        <result property="filePath" column="file_path"/>
    </resultMap>

    <select id="selectDetailsByExamAndStudent" resultMap="CountyExamPaperQuestionDetailResult">
        select question_id, question_score, order_num, question_content, question_type,
               option_a, option_b, option_c, option_d, answer, analysis, typing_duration,
               word_count, preview_path, file_path
        from biz_county_exam_paper_question
        where exam_id = #{examId}
          and student_id = #{studentId}
        order by order_num asc
    </select>
</mapper>
