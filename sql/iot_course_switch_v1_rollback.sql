-- =============================================================
-- 课程级物联网开关迁移 v1 回滚脚本（2026-08-21）
-- 用途：删除 biz_lesson.iot_enabled 字段，恢复到旧版「全局显示物联入口」行为。
-- 注意：回滚字段后必须同时回退前后端到旧版本，否则新代码查询该字段会报错。
-- 前置：执行前备份目标库 biz_lesson 表。
-- =============================================================

ALTER TABLE biz_lesson DROP COLUMN iot_enabled;
