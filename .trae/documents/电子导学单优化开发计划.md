# 电子导学单优化开发计划——30条建议完整实施方案

> **来源**：基于对 180 个前端文件 + 87 个后端 Java 文件 + 全部配置的深度审计
> **用户确认**：全部采纳 30 条建议
> **实施分 3 批**：P0 致命修复(6h) → P1 核心闭环(8h) → P2 体验打磨(6h)

---

## 总览

```
P0: 致命修复 (6项)  ──→  项目能跑、不崩溃
P1: 核心闭环 (11项) ──→  教师设计→学生填写→教师看进度→翻页控制
P2: 体验打磨 (13项) ──→  清理死代码、ECharts、导出、部署就绪
```

---

## P0：致命修复——让项目能跑

### S1 — 安装后端缺失依赖

**文件**：`e:\Project\newdazi\RuoYi-Vue\ruoyi-business\pom.xml`

**动作**：在 `<dependencies>` 中添加：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-websocket</artifactId>
</dependency>
<dependency>
    <groupId>com.alibaba.fastjson2</groupId>
    <artifactId>fastjson2</artifactId>
</dependency>
```

**验证**：`mvn -pl ruoyi-business -am compile` 通过

---

### S2 — 安装前端缺失依赖

**命令**：
```powershell
cd e:\Project\newdazi\RuoYi-Vue3
npm install survey-creator-knockout survey-core survey-knockout-ui knockout --save
```

**验证**：`package.json` 中新增 4 个依赖；`npm run build:prod` 通过

---

### S3 — 修复 dashboard.vue ESM 兼容性

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/dashboard.vue`

**改动**：
1. 删除第 151 行的 `require('@/utils/teacherMachine')` 
2. 在 `<script setup>` 顶部已有 `import { getListUrl } from '@/utils/teacherMachine'`，补充 `setTeacherMachineIp`：
   ```javascript
   import { getListUrl, setTeacherMachineIp } from '@/utils/teacherMachine'
   ```
3. 将 `onMounted` 中的 `const { setTeacherMachineIp } = require(...)` 移除，`setTeacherMachineIp(ip)` 直接调用顶层导入

---

### S4 — 激活 WebSocket 连接

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/dashboard.vue`

**改动**：
1. 在 `onMounted` 中，获取到 `sheetId` 后调用：
   ```javascript
   webSocketClient.connectClassroom(userStore.deptId, classCode.value)
   ```
2. 在 `onBeforeUnmount` 中已有的 `webSocketClient.disconnect()` 保留
3. 在 `refresh()` 函数开始时也确保连接（如果断线）：

---

### S5 — 修复 designer.vue 返回值字段

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/designer.vue`

**改动**：第 163 行 `res.sheetId` → `res.data?.sheetId`

---

### S6 — 修复 performance.vue API 传参

**文件**：`RuoYi-Vue3/src/views/business/teacher/performance.vue`

**改动**：第 133 行
```javascript
// 旧：
getPerformanceList(selectedLessonId.value, selectedClassCode.value, selectedEntryYear.value)

// 新：
getPerformanceList({ 
  lessonId: selectedLessonId.value, 
  classCode: selectedClassCode.value, 
  entryYear: selectedEntryYear.value 
})
```

---

## P1：核心闭环——功能可演示

### S7 — 删除死代码 `student/index1.vue`

**文件**：`RuoYi-Vue3/src/views/business/student/index1.vue` — 直接删除

---

### S8 — 删除死代码 `student/home.vue`

**文件**：`RuoYi-Vue3/src/views/business/student/home.vue` — 直接删除

---

### S9 — 删除死代码 `utils/student.js`

**文件**：`RuoYi-Vue3/src/utils/student.js` — 直接删除

---

### S10 — 移除未使用的 import

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/dashboard.vue`

**改动**：移除第 81 行 `import { getListUrl } from '@/utils/teacherMachine'` 中的 `getListUrl`（保留 `setTeacherMachineIp`）——因为 `getListUrl` 在 S3 中被移除后，这里可能需要重新组织导入。

---

### S11 — 数据库菜单 SQL 脚本

**新文件**：`e:\Project\newdazi\RuoYi-Vue\sql\menu_guide_sheet.sql`

**内容**：INSERT 语句向 `sys_menu` 注册导学单管理菜单及 7 个权限按钮：
- 菜单名「导学单管理」，图标 `guide-sheet`，路径 `/business/guide-sheet-list`
- 7 个子菜单按钮：查询/新增/修改/删除/设计/看板/导出
- 角色分配：关联 teacher 和 admin 角色

---

### S12 — 教师首页集成导学单

**后端改动**：
1. `TeacherDashboardController.java` — 在 `getDashboardData()` 中新增查询当前教师创建的导学单列表
2. `GuideSheetMapper.xml` — 新增查询方法按 `creatorId + deptId` 查导学单

**前端改动**：
1. `teacher/index.vue` — 在每个年级分组的课程卡片区域旁新增「导学单」标签页或独立卡片
2. 显示：标题、状态（草稿/已发布）、创建时间、快捷入口

---

### S13 — 设计器：班级改为下拉多选

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/designer.vue`

**改动**：
1. 获取学生所在的所有班级列表（调用已有接口或新增 API）
2. 将手动输入框 + 标签改为 `el-select` 多选下拉，选项来自班级列表
3. 移除手工添加/删除班级的函数和 UI

---

### S14 — 设计器关联课程下拉

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/designer.vue`

**改动**：
1. 关联课程从 `el-input` 改为 `el-select`，选项来自 `listLesson` API
2. 保存时将 `lessonId` 写入提交数据

---

### S15 — 学生端导学单文件上传集成 Flask

**文件**：`RuoYi-Vue3/src/views/student/guideSheet/index.vue`

**改动**：
1. 在 `renderSurvey` 回调中，对文件上传类型题目，拦截 SurveyJS 的上传行为
2. 文件通过 `el-upload` POST 到教师机 Flask `/upload`
3. 获取返回的 URL 后填入 SurveyJS 表单数据
4. 提交时一并调用 `confirmUpload` 记录元数据

---

### S16 — 学生端 WebSocket 翻页指令

**文件**：`RuoYi-Vue3/src/views/student/guideSheet/index.vue`

**改动**：
1. `onMounted` 中建立 WebSocket 连接到 `/ws/classroom/{deptId}/{classCode}`
2. 监听 `page_change` 事件：`window.Survey.Model.currentPage = page`
3. 监听 `refresh` 事件：重新加载导学单
4. 监听 `message` 事件：顶部 NotificationBar 显示教师消息

---

### S17 — 修复 Service 无用调用

**文件**：`RuoYi-Vue/ruoyi-business/src/main/java/com/ruoyi/business/service/impl/GuideSheetServiceImpl.java`

**改动**：`deleteBizGuideSheetBySheetIds` 中删除：
```java
guideSheetAnswerMapper.selectByStudentAndSheet(0L, sheetId);
```
改为：
```java
guideSheetAnswerMapper.deleteBizGuideSheetAnswerByAnswerIds(
    answerIds.toArray(new Long[0]));
```
即：删除前先查询 answer 的 ID 列表，然后批量删除

---

## P2：体验打磨——交付就绪

### S18 — 看板 ECharts 进度图表

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/dashboard.vue`

**改动**：
1. 引入 echarts，创建进度饼图（已提交/填写中/未开始）
2. 替换 3 个文本卡片为一行 ECharts 环形图
3. 增加各班级提交对比柱状图

---

### S19 — 看板作品展示 FLASK 轮询

**文件**：`RuoYi-Vue3/src/views/business/guideSheet/dashboard.vue`

**改动**：
1. 定时每 3 秒调用 `getListUrl(classCode)` 拉取教师机 Flask 的上传列表
2. 去重合并到 `uploads` 数组
3. 图片用 `<img>` 渲染，视频用 `<video>`，点击放大预览

---

### S20 — 导出导学单 Excel

**后端新增**：`GuideSheetController.java` 增加 `/export` 端点
1. 接收 `sheetId` + `classCode`
2. 查询所有填写记录
3. 解析 `answerJson`，按题目展开为列
4. 使用 `ExcelUtil` 导出

**前端**：列表页增加「导出」按钮，选择班级后调用导出 API

---

### S21 — 唯一约束友好处理

**文件**：`RuoYi-Vue/ruoyi-business/src/main/java/com/ruoyi/business/service/impl/GuideSheetAnswerServiceImpl.java`

**改动**：`saveAnswer` 中 catch `DuplicateKeyException`，返回友好提示"您已提交过该导学单，无需重复提交"

---

### S22 — WebSocket 超时清理

**文件**：`RuoYi-Vue/ruoyi-business/src/main/java/com/ruoyi/business/controller/ClassroomWebSocketController.java`

**改动**：
1. 在 `@OnOpen` 启动一个 Timer 定时检查心跳
2. 超过 5 分钟无心跳的连接自动关闭并从 classroomMap 中移除

---

### S23 — 导学单发布校验

**文件**：`RuoYi-Vue/ruoyi-business/src/main/java/com/ruoyi/business/service/impl/GuideSheetServiceImpl.java`

**改动**：`publishGuideSheet` 方法增加前置校验：
1. `surveyJson` 非空
2. 至少指派了一个班级
3. 不满足则抛出 `ServiceException` 提示具体原因

---

### S24 — 提交后不可编辑

**文件**：`RuoYi-Vue3/src/views/student/guideSheet/index.vue`

**改动**：如果 `existingAnswer.status === '2'`（已提交）：
1. SurveyJS 设置为 `mode: 'display'`
2. 隐藏提交和保存按钮
3. 显示"已提交"标签

---

### S25 — 断线保存进度

**文件**：`RuoYi-Vue3/src/views/student/guideSheet/index.vue`

**改动**：
1. `window.addEventListener('beforeunload', () => {...})` — 保存当前进度到后端
2. 每 30 秒自动保存草稿（不提示用户）
3. 网络断开时本地 localStorage 暂存答案

---

### S26 — 状态校验完整性

**文件**：`RuoYi-Vue/ruoyi-business/src/main/java/com/ruoyi/business/controller/GuideSheetController.java`

**改动**：
1. 发布操作检查当前状态是否为"草稿"
2. 关闭操作检查当前状态是否为"已发布"
3. 修改操作检查当前状态是否为"草稿"
4. 学生查看时过滤 `status != '1'` 的导学单

---

### S27 — WebSocket 配置文档

**文件**：`RuoYi-Vue/ruoyi-admin/src/main/resources/application.yml`

**改动**：添加注释说明 WebSocket 路径 `/ws/classroom/**`

---

### S28 — 一键构建脚本

**新文件**：`e:\Project\newdazi\RuoYi-Vue\build-all.bat`

**内容**：
```batch
echo === 开始构建后端 ===
call mvn -pl ruoyi-admin -am -DskipTests clean package
echo === 开始构建前端 ===
cd ..\RuoYi-Vue3
call npm run build:prod
echo === 构建完成 ===
pause
```

---

### S29 — 部署检查清单

**新文件**：`e:\Project\newdazi\contexts\deploy-checklist.md`

**内容**：
- 数据库脚本执行检查（5张表 + 菜单SQL）
- 依赖安装（后端Maven + 前端npm）
- Nginx 配置确认
- LibreOffice 路径确认
- 防火墙端口 5000 开放
- 教师机 Flask 程序启动
- 角色权限分配

---

### S30 — 更新 context.md

**文件**：`e:\Project\newdazi\contexts\context.md`

**改动**：
1. 版本号更新为 v3.0
2. 新增「电子导学单模块」章节：
   - 功能概述（教师设计→学生填写→课堂控制→数据看板→作品展示）
   - 5 张新表的简要说明
   - 与现有课程体系的集成关系
   - 权限字符串清单
3. 更新开发路线图中的 P2 项目复选框
4. 修正第13行「MyBatis-Plus」为「MyBatis」（与代码实际一致）

---

## 执行顺序与依赖关系

```
P0（可并行）:
  S1(后端依赖) ←→ S2(前端依赖) ←→ S3/S4/S5/S6(Bug修复)
                                  ↓
P1（依赖 P0 完成）:
  S7/S8/S9(死代码删除) | S11(菜单SQL)
  S10(移除未用导入)    | S13/S14(设计器 UX)
  S17(修复Service)     | S12(教师首页集成)
                       | S15/S16(学生端集成)
                                  ↓
P2（依赖 P1 完成）:
  S18(ECharts) | S19(Flask轮询) | S20(导出)
  S21(异常)    | S22(超时清理)  | S26(状态校验)
  S23(发布校验)| S24(只读模式)  | S25(断线保存)
  S27(文档)    | S28(构建脚本)  | S29(部署清单) | S30(context更新)
```

## 验证标准

完成每批次后必须通过的验证：

**P0 验证**：
- `mvn -pl ruoyi-admin -am -DskipTests clean package` 成功
- `npm run build:prod` 成功
- 后端启动无异常日志
- 前端登录无控制台错误

**P1 验证**：
- 教师能创建导学单→设计表单→指派班级→发布
- 学生能看到导学单→填写→保存草稿→提交
- 教师看板能显示进度→翻页按钮能发送 WebSocket 消息
- 管理列表可搜索/删除/发布/关闭

**P2 验证**：
- 看板 ECharts 图表正常渲染
- 导出 Excel 包含所有填写数据
- 断网后恢复，已填写内容不丢失
- 构建脚本一键成功
